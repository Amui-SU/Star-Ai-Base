# 智库云移动端大版本完善执行方案

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在不打断现有可用版本的前提下，把智库云移动端从“功能可跑”推进到“可稳定分发、可持续迭代、体验闭环清晰”的大版本基础。

**Architecture:** 先稳住配置、权限、任务和模块边界，再进入对话历史、学习复盘、分 P 与多向量后端等产品功能。后端保持 FastAPI + SQLite + Chroma 的本地优先架构，前端保持 Next.js 静态导出 + Capacitor Android 的移动端交付方式。

**Tech Stack:** FastAPI, SQLAlchemy async, SQLite, ChromaDB, LangChain, DashScope/OpenAI-compatible LLM, Next.js 16, React 19, TypeScript, Capacitor Android, Vitest, Pytest.

---

## 1. 当前状态结论

项目已经不是早期原型，主流程具备继续做大版本的基础：

- 后端测试健康：`python -m pytest` 为 205 passed。
- 前端单测健康：`npm test` 为 21 个测试文件、119 个测试通过。
- 前端生产构建健康：`npm run build` 可完成静态导出。
- Windows 启动器健康：`scripts/dev.ps1 doctor` 检测 Python、Node、npm、前后端依赖、ffmpeg、logs 均通过。
- 本地服务健康：后端 `/health` 返回 healthy，前端 `http://127.0.0.1:3000` 返回 200。

当前短板集中在四类：

- 工程交付：环境变量模板、lint 扫描范围、发布文档与 APK 交付说明需要更清楚。
- 权限边界：模型配置、联网搜索配置现在是全局设置，适合本机个人使用，不适合多人共同使用。
- 长任务可靠性：入库任务已有 `IngestionTask` 持久化状态，但执行仍依赖进程内 `BackgroundTasks`；服务重启后会把旧任务标记为 `interrupted`，但不会自动恢复或重试。
- 可维护性：`ChatPanel.tsx`、`globals.css`、`chat.py`、`knowledge_bases.py`、`knowledge.py` 已经偏大，继续加功能前需要拆分边界。

## 2. 本次执行范围

用户明确要求：

- 在 `G:\gitbase\智库云 - 手机版\star-base-main\docs` 写一份详细完善文档。
- 开始执行。
- “建议 1”暂时不用整体展开。
- 将 `.env` 改成 `.env.example`。

本次实际执行只包含：

- [x] 新增本完善文档：`docs/大版本完善执行方案.md`。
- [x] 将仓库模板 `.env` 改为 `.env.example`。
- [x] 修改 `.gitignore`，让真实 `.env`、`.env.local`、`.env.*.local` 默认不入库。
- [x] 修复前端 lint 扫描 Android/Gradle 本地产物的问题。
- [x] 新增移动端发布检查清单。
- [x] README 增加 `.env.example` 复制为 `.env.local` 的说明。
- [x] 完成 P1 Task 4：模型配置与联网搜索配置权限收口。
- [x] 完成 P1 Task 4.5：用户自带 AI 服务密钥管理。

本次仍暂缓：

- [ ] 不调整 `docs/` 的 git ignore 策略。
- [ ] 不拆分大文件。
- [ ] 不引入任务队列。
- [ ] 不做大规模 UI/产品重构。

## 3. 优先级总览

### P0：大版本前必须清掉的交付风险

1. 环境变量模板化。
2. 修复前端 lint 扫描 Android/Gradle 本地产物的问题。
3. 明确“本机个人模式”和“多人/外发模式”的配置边界。
4. 对 APK、启动器、后端配置形成一页式发布检查清单。

### P1：大版本实施前建议先做的结构整理

1. 模型配置权限收口。
2. 用户自带 AI 服务密钥管理。
3. 入库/导入长任务恢复机制。
4. 把旧全局知识库逻辑抽离到 service 层。
5. 拆分聊天前端组件与全局 CSS。
6. 移动端空状态、异常状态和编码显示问题整理。

### P2：适合作为“大过年功能更新”的产品卖点

1. 对话历史与检索历史。
2. 知识库摘要、视频摘要与学习复盘卡片。
3. B 站分 P 视频支持。
4. 更多 Embedding / 向量后端 / 本地模型适配。
5. 移动端离线阅读、收藏摘要与复习提醒。

## 4. P0 执行计划

### Task 1: 环境变量模板化

**Files:**

- Create: `.env.example`
- Delete: `.env`
- Modify: `.gitignore`

**目标：**

仓库只保存可公开的配置模板，真实运行配置留在 `.env` 或 `.env.local`，避免以后误提交 API Key、OAuth secret、SMTP password 等敏感信息。

**执行步骤：**

- [x] 创建 `.env.example`，保留本地运行所需的键名和默认值。
- [x] 所有 secret 类字段保持空值，例如 `DASHSCOPE_API_KEY=`、`GOOGLE_CLIENT_SECRET=`。
- [x] 删除仓库根目录旧 `.env`。
- [x] `.gitignore` 改为忽略 `.env`、`.env.local`、`.env.*.local`。
- [x] README 增加“复制 `.env.example` 为 `.env.local`”的说明。

**验收标准：**

- `Test-Path .env` 返回 `False`。
- `Test-Path .env.example` 返回 `True`。
- `git status --short` 能看到 `.env` 删除、`.env.example` 新增、`.gitignore` 修改。
- `.env.example` 中没有真实密钥值。

### Task 2: 修复 lint 扫描范围

**Files:**

- Modify: `frontend/eslint.config.mjs`

**问题：**

`npm run lint` 当前会扫描 `frontend/android/.gradle-user-home` 里的 Gradle/Javadoc 第三方 JS，导致大量与业务无关的 lint error。

**建议改法：**

在 `globalIgnores` 中补充：

```js
"android/.gradle-user-home/**",
"android/**/build/**",
"android/capacitor-cordova-android-plugins/**",
```

**测试：**

```powershell
cd frontend
npm run lint
```

**验收标准：**

- [x] `npm run lint` 不再进入 Gradle 缓存、Android build 输出、Cordova generated plugins。
- [x] 如果业务代码存在 lint 问题，输出应只指向 `app/`、`components/`、`lib/` 等项目源码。

**执行记录：**

- [x] 已在 `frontend/eslint.config.mjs` 添加 `android/.gradle-user-home/**`、`android/**/build/**`、`android/capacitor-cordova-android-plugins/**`。
- [x] 已运行 `npm run lint`，结果通过。

### Task 3: 发布检查清单

**Files:**

- Create: `docs/移动端发布检查清单.md`
- Optional Modify: `README.md`
- Optional Modify: `frontend/APK-打包说明.md`

**目标：**

每次打包 APK 前有一套固定检查，减少“本机能用、换设备就连不上”的情况。

**清单内容建议：**

- 后端是否监听 `0.0.0.0:8000`。
- 手机浏览器是否能访问 `http://<电脑局域网 IP>:8000/health`。
- `.env.local` 是否配置目标模型 Key、ASR Key、`APP_ENCRYPTION_KEY`。
- `scripts/dev.ps1 doctor` 是否全绿。
- `npm test`、`npm run build` 是否通过。
- APK 安装后是否能扫码保存连接。
- 移动端登录、创建知识库、绑定 B 站、导入、提问是否完成一次冒烟测试。

**验收标准：**

- [x] 新成员或未来自己可以只按清单执行一次发布验证。

**执行记录：**

- [x] 已新增 `docs/移动端发布检查清单.md`。
- [x] 已覆盖配置检查、本机服务检查、网络连接检查、自动化验证、APK 构建前检查、真机冒烟测试、发布物检查。

## 5. P1 执行计划

### Task 4: 模型配置权限收口

**Files:**

- Modify: `app/routers/chat.py`
- Modify: `frontend/components/ChatPanel.tsx`
- Modify: `frontend/components/UserMenu.tsx` 或新增设置入口
- Test: `tests/test_system_auth.py` 或新增 `tests/test_llm_config_permissions.py`
- Test: `frontend/components/ChatPanel.test.tsx`

**问题：**

`/chat/llm/provider-config` 与 `/chat/web-search/config` 会写入全局 `.env.local`。当前依赖 `get_current_user`，任何已登录用户都可能修改全局模型配置。

**建议方案：**

先按本机产品形态做最小收口：

- 管理员可以修改全局模型配置。
- 普通用户只能查看当前模型状态、选择是否开启联网搜索，但不能写入 API Key。
- 前端把“配置模型”和“配置联网搜索 Key”按钮对普通用户隐藏或置灰，并给出“需要管理员配置”的状态提示。

**测试：**

新增后端测试：

- [x] 普通用户 POST `/chat/llm/provider-config` 返回 403。
- [x] 普通用户 POST `/chat/llm/config` 返回 403。
- [x] 普通用户 POST `/chat/web-search/config` 携带 API Key 返回 403。
- [x] 管理员 POST `/chat/llm/provider-config` 保持原行为。
- [x] 管理员可保存 Tavily Key。

**验收标准：**

- [x] 多用户场景中，普通用户不能覆盖全局密钥。
- [x] 本机管理员仍能在界面完成模型配置。
- [x] 普通用户界面不展示全局模型配置和 Tavily Key 配置入口。

**执行记录：**

- [x] `app/routers/chat.py` 的全局配置写入接口已改为管理员权限依赖。
- [x] `frontend/app/page.tsx` 已把 `systemUser.is_admin` 传入 `ChatPanel`。
- [x] `ChatPanel` 与 `ChatScopePicker` 已按管理员状态控制模型配置、模型切换、Tavily 配置入口。
- [x] 已新增 `tests/test_chat_config_permissions.py`。
- [x] 已更新 `ChatPanel.test.tsx` 与 `ChatScopePicker.test.tsx` 覆盖普通用户隐藏配置入口、管理员配置 Tavily 的路径。

### Task 4.5: 用户自带 AI 服务密钥管理

**Design:**

- 用户自带 AI 服务密钥设计已完成并并入本执行方案；完成后的临时设计/执行文档不再保留。

**Files:**

- Create: `app/services/api_credentials.py`
- Modify: `app/models.py`
- Modify: `app/database.py` 或迁移初始化逻辑
- Modify: `app/routers/chat.py`
- Create: `app/routers/api_accounts.py`
- Modify: `app/services/web_search.py`
- Modify: `frontend/components/UserMenu.tsx`
- Create: `frontend/components/ApiAccountsPanel.tsx`
- Modify: `frontend/components/ChatPanel.tsx`
- Test: `tests/test_user_api_accounts.py`
- Test: `tests/test_chat_user_credentials.py`
- Test: `frontend/components/ApiAccountsPanel.test.tsx`
- Test: `frontend/components/ChatPanel.test.tsx`

**决策：**

不再专门区分“本地部署模式”和“线上 Web 模式”。智库云统一作为多用户 Web 应用设计：本地部署只是用户自己运行这套 Web 应用，线上部署只是放到服务器上运行，AI 服务密钥逻辑不分叉。

**目标：**

正式产品路径采用“用户自带 AI 服务密钥”为主。用户登录后添加自己的 DeepSeek、OpenAI、DashScope、Kimi、SiliconFlow、Zhipu、Tavily 等服务密钥，后续聊天、摘要、复盘、联网搜索优先使用当前用户自己的凭证。

同时在模型选择中预留“官方 / 个人”来源切换：`个人` 使用用户自己的 AI 服务密钥；`官方` 使用管理员配置的全局模型 Key，作为未来平台付费通道占位。现阶段不接入支付，不做扣费，官方通道没有全局 Key 时显示未开通。

**配置：**

```env
API_ACCOUNT_MODE=user_required
PLATFORM_API_ENABLED=false
BILLING_ENABLED=false
```

现阶段不启用平台代付费，不做套餐扣费，只预留 `usage_events` 记录调用量。未来收费时再扩展为 `API_ACCOUNT_MODE=hybrid`、`PLATFORM_API_ENABLED=true`、`BILLING_ENABLED=true`。

**后端设计：**

- 新增 `user_api_accounts`，API Key 用 `APP_ENCRYPTION_KEY` 加密保存。
- 新增 `usage_events`，记录用户、provider、model、feature、tokens、状态和错误。
- 新增 `ApiCredentialResolver`，所有 AI 调用只通过它获取凭证。
- `SystemUser.llm_api_source` 保存当前用户的模型来源偏好：`personal` 或 `official`。
- Resolver 按用户选择解析凭证：`personal` 读取当前用户启用的 LLM 账号；`official` 读取管理员全局模型配置，缺失时返回 `official_api_required`。
- 管理员全局配置接口保留为开发、兼容、排障和未来官方付费通道占位，普通用户不能写入全局配置。

**前端设计：**

- 用户菜单新增“AI 服务密钥”。
- 模型选择弹窗顶部新增“官方 / 个人”切换。
- 未配置个人 AI 服务密钥且当前来源为 `个人` 时，聊天区提示“先添加 AI 服务密钥”。
- 当前来源有可用模型时，聊天页不再显示未配置提示。
- 模型选择弹窗显示当前来源下可用的 provider；官方未开通时标记为“未开通”，个人未配置时标记为“未配置”。
- 普通用户不显示全局模型配置或全局 Tavily Key 配置。

**验收标准：**

- [x] 用户可以新增、验证、编辑、删除自己的 AI 服务密钥。
- [x] API Key 不会在任何响应体中明文返回。
- [x] 用户不能访问或修改其他用户的 AI 服务密钥。
- [x] 没有 AI 服务密钥时，聊天返回稳定错误码 `api_account_required`。
- [x] 有默认 AI 服务密钥时，聊天使用当前用户自己的 Key。
- [x] Tavily 联网搜索使用当前用户自己的 Tavily Key。
- [x] 每次聊天 AI 调用写入 `usage_events`。
- [x] 用户可以在模型选择中切换官方 / 个人来源。
- [x] 官方来源未配置全局 Key 时返回稳定错误码 `official_api_required`。
- [x] 普通用户无法写入管理员全局 API 配置。
- [x] AI 服务密钥弹窗在桌面与移动端不被裁切。

**执行记录：**

- [x] 已新增 `user_api_accounts`、`usage_events` 模型和 `/api-accounts` 用户作用域 CRUD / validate / set-default 接口，前端命名为“AI 服务密钥”。
- [x] 已新增 `app/services/api_credentials.py`，聊天链路改为解析当前用户自己的 LLM 账号；没有账号时返回 `api_account_required`。
- [x] `/chat/llm/config` 和 `/chat/web-search/config` 已反映当前用户自己的账号配置状态，不返回 API Key 明文。
- [x] Tavily 搜索已支持请求级 `tavily_api_key` 覆盖，知识库联网搜索会读取当前用户 Tavily 账号。
- [x] 前端已新增 `ApiAccountsPanel`，用户菜单增加 `AI 服务密钥` 入口，聊天空状态增加配置引导。
- [x] 已新增 `/chat/llm/source` 与 `SystemUser.llm_api_source`，聊天、健康检查、知识库 scoped chat 会按官方 / 个人来源解析凭证。
- [x] 模型选择 UI 顶部已增加“官方 / 个人”切换；官方预留付费通道，个人使用用户自带 Key。
- [x] 已修复 `AI 服务密钥` 弹窗桌面端宽度、弹窗层级和移动端单列滚动适配。
- [x] `.env.example` 已预留 `API_ACCOUNT_MODE=user_required`、`PLATFORM_API_ENABLED=false`、`BILLING_ENABLED=false`。
- [x] 验证通过：`python -m pytest -q` 为 211 passed。
- [x] 验证通过：`cd frontend; npm test` 为 22 个测试文件、126 tests passed。
- [x] 验证通过：`cd frontend; npm run lint`。
- [x] 验证通过：`cd frontend; npm run build`。
- [x] 2026-06-25 补充验证通过：`python -m pytest tests/test_chat_user_credentials.py tests/test_chat_config_permissions.py tests/test_user_api_accounts.py tests/test_knowledge_base_scoping.py tests/test_knowledge_scope.py -q` 为 84 passed。
- [x] 2026-06-25 补充验证通过：`cd frontend; npm test -- ChatPanel.test.tsx ApiAccountsPanel.test.tsx api-accounts-layout.test.ts mobile-chat-layout.test.ts ChatScopePicker.test.tsx UserMenu.test.tsx api.test.ts` 为 7 个测试文件、63 tests passed。

### Task 5: 入库/导入任务恢复

**Files:**

- Create: `app/services/ingestion_tasks.py`
- Modify: `app/routers/knowledge_bases.py`
- Modify: `app/routers/imports.py`
- Modify: `app/main.py`
- Test: `tests/test_imports.py`
- Test: `tests/test_knowledge_base_scoping.py`

**问题：**

`IngestionTask` 已经持久化任务状态，但任务执行依赖进程内 `BackgroundTasks`。服务重启后，`pending/running` 旧任务不会自动恢复或重试；当前维护边界是启动时标记为 `interrupted`，让用户明确看到中断并重新发起。

**建议方案：**

先做轻量恢复，不引入 Redis/Celery：

- 启动时扫描 `ingestion_tasks` 中 `pending/running` 且更新时间过旧的任务。
- 将无法恢复的任务标记为 `failed` 或 `interrupted`，提示用户重新执行。
- 对 URL 导入、本地视频导入、收藏夹构建保留重新发起入口。

**后续增强：**

- 如果未来要做真正断点续跑，再引入独立 worker 或 SQLite-backed queue。

**测试：**

- 构造一个 `running` 且 `updated_at` 很旧的任务，启动恢复函数后状态变为 `failed` 或 `interrupted`。
- 构造一个刚更新的 `running` 任务，恢复函数不处理。
- 确认任务状态接口仍按知识库归属校验。

**验收标准：**

- [x] 服务重启后，界面不会长期卡在“运行中”。
- [x] 用户能看懂任务中断原因，并重新发起。

**执行记录：**

- [x] 已新增 `app/services/ingestion_tasks.py`，启动时可将超时的 `pending/running` 任务标记为 `interrupted`。
- [x] `app/main.py` 的 lifespan 已在数据库初始化后执行任务恢复扫描。
- [x] `SourcesPanel` 已识别 `interrupted` 状态并提示用户重新发起。
- [x] 已新增 `tests/test_ingestion_tasks.py` 覆盖 stale task 恢复和 lifespan 调用。
- [x] 已更新 `frontend/components/SourcesPanel.test.tsx` 覆盖中断提示。
- [x] 任务创建、状态展示和状态更新已收口到 `app/services/ingestion_tasks.py`，router 不再保留局部任务更新 helper。

### Task 6: 旧全局知识库逻辑抽离

**Files:**

- Create: `app/services/rag_runtime.py`
- Create: `app/services/folder_ingestion.py`
- Modify: `app/routers/knowledge.py`
- Modify: `app/routers/knowledge_bases.py`
- Modify: `app/routers/imports.py`
- Test: `tests/test_knowledge_base_scoping.py`
- Test: `tests/test_imports.py`

**问题：**

`knowledge_bases.py` 和 `imports.py` 还从 `app.routers.knowledge` 复用 `_sync_folder` 和 `get_rag_service`。路由层互相依赖会让 legacy 代码长期绑住新主流程。

**建议方案：**

- `get_rag_service()` 移到 `app/services/rag_runtime.py`。
- `_sync_folder()` 移到 `app/services/folder_ingestion.py`。
- `knowledge.py` 只保留 legacy route wrapper。
- `knowledge_bases.py` 和 `imports.py` 直接依赖 service 层。

**测试：**

```powershell
python -m pytest tests/test_knowledge_base_scoping.py tests/test_imports.py
```

**验收标准：**

- [x] scoped 主流程不再 import legacy router。
- [x] legacy route 返回 410 的行为不变。
- [x] 入库、导入、检索测试全部通过。

**执行记录：**

- [x] 已新增 `app/services/rag_runtime.py`，集中管理 RAG service 单例与重置。
- [x] 已新增 `app/services/folder_ingestion.py`，承接收藏夹同步与 scoped 入库逻辑。
- [x] `knowledge_bases.py`、`imports.py`、`chat.py` 已改为依赖 service 层，不再 import `app.routers.knowledge`。
- [x] `knowledge.py` 已瘦身为 legacy route wrapper，并保留 `_sync_folder` 兼容导出。
- [x] 已新增 `tests/test_service_boundaries.py` 防止主流程 router 重新依赖 legacy router。

### Task 7: 前端聊天模块拆分

**Files:**

- Modify: `frontend/components/ChatPanel.tsx`
- Create: `frontend/components/chat/MessageList.tsx`
- Create: `frontend/components/chat/Composer.tsx`
- Create: `frontend/components/chat/WebSearchConfigModal.tsx`
- Create: `frontend/components/chat/MarkdownCode.tsx`
- Create: `frontend/components/chat/types.ts`
- Create: `frontend/components/chat/ModelConfigModal.tsx`
- Create: `frontend/components/chat/useChatStreaming.ts`
- Test: `frontend/components/ChatPanel.test.tsx`

**问题：**

`ChatPanel.tsx` 当前同时负责消息状态、流式解析、模型配置、联网搜索配置、范围选择、消息渲染、输入框和多个弹窗。继续加对话历史、复习卡片、摘要会显著增加回归风险。

**建议方案：**

分两阶段做：

1. 纯拆分，不改交互。
2. 拆分后再加对话历史和学习复盘。

**验收标准：**

- ChatPanel 主文件显著变短，只负责组合状态和组件。
- 现有 `ChatPanel.test.tsx` 全过。
- 移动端聊天页面视觉不变。

**执行记录：**

- [x] 已新增 `tests/test_frontend_structure.py`，防止聊天主面板重新内联消息列表、输入区和联网搜索配置弹窗。
- [x] 已将消息渲染拆到 `frontend/components/chat/MessageList.tsx`。
- [x] 已将输入框、范围选择和发送按钮拆到 `frontend/components/chat/Composer.tsx`。
- [x] 已将 Tavily 联网搜索配置弹窗拆到 `frontend/components/chat/WebSearchConfigModal.tsx`。
- [x] 已将 Markdown 代码块渲染拆到 `frontend/components/chat/MarkdownCode.tsx`，并将 `Message`、`Reaction` 移到 `frontend/components/chat/types.ts`，避免子组件反向依赖 `ChatPanel`。
- [x] 已恢复拆分组件中的中文文案，避免拆分过程引入乱码回归。
- [x] 已将模型配置弹窗拆到 `frontend/components/chat/ModelConfigModal.tsx`。
- [x] 已将消息状态、流式请求、编辑重发、重新生成、复制与反馈逻辑抽到 `frontend/components/chat/useChatStreaming.ts`。
- [x] 对抗检查确认：源码为 UTF-8，PowerShell 无 BOM 读取时的中文乱码只是控制台显示问题；Python UTF-8 读取、lint、build 均正常。
- [x] 验证通过：`python -m pytest tests/test_frontend_structure.py -q` 为 1 passed。
- [x] 验证通过：`cd frontend; npm test -- ChatPanel.test.tsx` 为 24 passed。
- [x] 验证通过：`cd frontend; npm test` 为 22 个测试文件、127 tests passed。
- [x] 验证通过：`cd frontend; npm run lint`。
- [x] 验证通过：`cd frontend; npm run build`。

### Task 8: 移动端空状态与编码问题

**Files:**

- Modify: `frontend/components/KnowledgeBasePanel.tsx`
- Modify: `frontend/components/SourcesPanel.tsx`
- Modify: `frontend/components/ChatPanel.tsx`
- Modify: `frontend/app/globals.css`
- Test: `frontend/app/mobile-chat-layout.test.ts`
- Test: related component tests

**问题：**

已有 QA 截图中出现知识库名称 `????????`，这类编码或 fallback 显示会明显降低产品可信度。空状态也需要更明确地引导用户下一步。

**建议方案：**

- 对知识库名称、收藏夹名称、视频标题统一做 fallback：空值显示“未命名知识库/未命名收藏夹/未命名视频”。
- 对可能来自后端或 B 站的异常字符串做显示层兜底。
- 空状态根据流程分级：
  - 未选知识库：提示先创建或选择知识库。
  - 已选知识库但无资料：提示绑定内容源或导入视频。
  - 有资料但无检索结果：提示切换范围或打开联网搜索。

**验收标准：**

- 移动端不出现裸 `????` 或空白标题。
- 关键空状态都有下一步入口。

**执行记录：**

- [x] 已新增 `frontend/lib/displayNames.ts`，统一处理空白、全问号和替换字符标题。
- [x] `KnowledgeBasePanel`、`SourcesPanel`、`ChatScopePicker`、`ChatPanel` 已接入知识库、收藏夹、视频标题兜底。
- [x] 已新增 `frontend/components/KnowledgeBasePanel.test.tsx`，并扩展 `SourcesPanel.test.tsx`、`ChatScopePicker.test.tsx` 覆盖空标题与 `????`。
- [x] TDD 红灯已确认：新增目标测试在实现前因裸 `????`、空白标题失败。
- [x] 验证通过：`cd frontend; npm test -- KnowledgeBasePanel.test.tsx SourcesPanel.test.tsx ChatScopePicker.test.tsx` 为 3 files / 16 tests passed。
- [x] 验证通过：`cd frontend; npm test` 为 23 files / 130 tests passed。
- [x] 验证通过：`cd frontend; npm run lint`。
- [x] 验证通过：`cd frontend; npm run build`。

## 6. P2 产品功能路线

### Route A: 学习复盘体验版

适合春节大版本主卖点。

**核心能力：**

- [x] 对话历史与会话管理（第一版：用户隔离、按知识库筛选、恢复范围与联网搜索上下文）。
- 检索历史。
- 视频学习摘要。
- 知识库总览。
- 一键生成复习卡片。

**为什么优先：**

这条路线最贴合“把收藏夹变成可复盘知识库”的定位，用户能明显感知升级，而不是只感知工程稳定性。

### Route B: 内容能力扩展版

适合做技术声量。

**核心能力：**

- 支持 B 站分 P 视频。
- 支持更多导入来源。
- 支持更多 Embedding 和向量后端。
- 支持开源本地模型。

**风险：**

会明显增加索引结构、任务队列、成本控制和 UI 选择复杂度。建议在 P1 完成后再做。

### Route C: 移动端体验增强版

适合提升日常使用频率。

**核心能力：**

- 移动端首页改成最近知识库和最近对话。
- 快速提问入口。
- 导入任务通知。
- 失败任务重试。
- 连接设置诊断。

**风险：**

需要较多真实设备验证，尤其是 Android 网络、扫码和文件上传。

## 7. 推荐排期

### 第 1 阶段：1-2 天

- [x] 完成 `.env.example`。
- [x] 修复 lint 扫描范围。
- [x] 写发布检查清单。
- [x] 运行全量后端/前端测试。

### 第 2 阶段：2-4 天

- [x] 模型配置权限收口。
- [x] 用户自带 AI 服务密钥管理。
- [x] 任务中断状态恢复。
- [x] legacy service 抽离。

### 第 3 阶段：3-5 天

- [x] 拆 ChatPanel。
- [x] 移动端空状态与异常状态整理。
- [ ] 做一次手机真机回归。

### 第 4 阶段：5-10 天

- [x] 对话历史第一版：用户隔离、最近对话、恢复上下文、继续聊天。
- [ ] 做学习复盘体验版剩余部分：检索历史、知识库摘要、复习卡片。

## 8. 验证矩阵

每个阶段至少执行：

```powershell
python -m pytest
cd frontend
npm test
npm run build
npm run lint
```

移动端变更还需要：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\dev.ps1 doctor
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\dev.ps1 status
```

真机手动验证：

- 登录。
- 连接设置扫码。
- 创建知识库。
- 绑定 B 站。
- 导入一条短视频或选择一个小收藏夹。
- 等待入库完成。
- 提问并检查来源展示。
- 切换暗色/亮色。
- 断网或后端停止时检查错误提示。

## 9. 风险清单

| 风险 | 影响 | 建议 |
| --- | --- | --- |
| 全局模型配置被普通用户覆盖 | 多用户混用时互相影响 | 已完成 P1 Task 4 管理员权限收口 |
| 正式产品继续依赖全局 API | 线上部署会产生费用归属和滥用风险 | P1 Task 4.5 已改为个人密钥为主，并通过“官方 / 个人”显式切换预留付费通道 |
| 长视频 ASR 或收藏夹入库中断 | 用户以为任务卡死 | P1 Task 5 标记 interrupted 或 failed |
| legacy route 与新 scoped route 互相引用 | 后续开发容易误改旧流程 | P1 Task 6 抽 service |
| ChatPanel 大文件维护压力 | 新功能回归风险高 | P1 Task 7 已拆出流式 hook、消息列表、输入区、弹窗和空状态组件 |
| 移动端出现乱码/问号标题 | 观感不专业 | P1 Task 8 加显示兜底 |
| lint 扫第三方缓存 | CI 无法使用 lint 作为质量门 | P0 Task 2 修 ignore |
| OAuth state 回调复用 | OAuth 回调可能被跨浏览器复用 | 已通过 HttpOnly/SameSite=Lax 临时 nonce cookie 绑定 state，成功登录后清理 |
| OAuth 回调异常裸打印 traceback | 异常堆栈绕过统一日志系统，排障和日志脱敏策略不一致 | Google OAuth 异常分支已改为 `logger.exception`，并加边界测试禁止 router 直接 `traceback.print_exc` |
| OAuth 网络异常回显上游错误 | httpx 异常文本可能包含请求参数或 provider secret，直接写入响应/日志会扩大泄露面 | WeChat/QQ/Google OAuth 网络异常已统一为固定 502 文案，日志只记录 provider 与异常类型 |
| 验证码发送频率绕过 | 多 worker、进程重启或公网暴力请求可能绕过单层应用限流并消耗 SMTP 资源 | 应用内已改为数据库持久化窗口计数；生产部署已要求网关限流或反向代理限流覆盖 `POST /system-auth/send-code`，按真实客户端 IP 做 per-IP 限制并保留可信 `X-Forwarded-For` |
| 验证码明文进入日志 | SMTP 未配置或发送异常时日志若包含验证码，会扩大凭据暴露面 | SMTP 未配置日志已去除验证码明文；DEBUG 模式仍仅通过接口响应返回验证码 |
| B 站二维码 pending state 共享 | 多 worker 下生成二维码与轮询命中不同进程会误报过期 | 旧 `/auth/qrcode` 与新 scoped `/source-bindings/bilibili/qrcode` pending state 均已持久化到数据库；`login_sessions` 仅作热缓存 |
| 旧向量删除失败后继续重建 | 向量库锁定或删除失败时可能追加重复向量 | scoped 收藏夹同步已改为删除旧向量成功后才写入新向量；失败时保留 DB 记录并记录 warning |
| 知识库删除时向量清理失败 | 若先删除 DB 记录但留下向量，SQLite id 复用后可能混入孤儿向量 | 删除知识库已改为先成功清理向量才删除 DB；向量清理失败时返回可重试错误并保留知识库 |
| 收藏夹端点异常路径资源泄漏 | 上游 B 站请求或响应组装异常时可能留下未关闭的 HTTP 客户端 | `favorites.py` 5 个端点已改为 `finally` 关闭 BilibiliService，并新增异常路径回归测试 |
| SQLite legacy 迁移入口过大 | 数据库兼容迁移持续堆叠时容易引入顺序和职责混乱 | 已将 `_ensure_sqlite_legacy_columns` 拆成模块级迁移 helpers，入口只负责 SQLite 判定和调用同步迁移流程 |
| Windows BAT 文档与仓库不一致 | 新用户按 README 双击启动器会找不到文件 | 已补齐根目录中文 BAT 包装器，并用 README 一致性测试保护其委托到 `scripts\dev.ps1` |
| ASR 服务测试覆盖不足 | 本地视频导入依赖 ASR，结果解析和模型切换缺少轻量单元测试会增加回归风险 | 已补充 ASR 结果解析、base URL 拼接、Recognition 输入格式选择和临时模型恢复测试 |

## 10. 当前执行记录

2026-06-25：

- 新增本文件。
- 将仓库根目录 `.env` 改为 `.env.example`。
- `.env.example` 不包含真实 API Key 或 secret。
- `.gitignore` 开始忽略真实 `.env` 文件。
- README 已改为推荐复制 `.env.example` 到 `.env.local`。
- `frontend/eslint.config.mjs` 已忽略 Android/Gradle 生成产物，`npm run lint` 已通过。
- 已新增 `docs/移动端发布检查清单.md`。
- 已完成模型配置权限收口：普通用户不能写入 LLM provider、LLM current provider、Tavily 配置；管理员保持原配置能力。
- 已完成前端权限联动：普通用户隐藏模型配置按钮，Tavily 未配置时提示需要管理员配置；管理员仍可打开配置弹窗。
- 验证通过：`python -m pytest -q` 为 195 passed。
- 验证通过：`cd frontend; npm test` 为 20 个测试文件、113 passed。
- 验证通过：`cd frontend; npm run lint`。
- 验证通过：`cd frontend; npm run build`。
- 已确定后续 API 方向：统一多用户 Web 应用，不再专门区分本地/线上模式，正式路径采用用户自带 AI 服务密钥。
- 已新增用户自带 AI 服务密钥管理设计，并在完成后清理临时设计/执行文档。
- 已实现模型来源切换：模型选择 UI 顶部提供“官方 / 个人”，个人来源读取用户自带 Key，官方来源读取管理员全局模型配置并预留未来付费通道。
- 已修复 AI 服务密钥配置弹窗裁切问题：桌面端加宽并修复 `.modal-card` 覆盖，移动端改为单列滚动，弹窗层级高于聊天状态控件和用户菜单。
- 补充验证通过：`python -m pytest tests/test_chat_user_credentials.py tests/test_chat_config_permissions.py tests/test_user_api_accounts.py tests/test_knowledge_base_scoping.py tests/test_knowledge_scope.py -q` 为 82 passed。
- 补充验证通过：`cd frontend; npm test` 为 22 个测试文件、125 passed。
- 补充验证通过：`cd frontend; npm run lint`。
- 补充验证通过：`cd frontend; npm run build`。
- 已完成 P1 Task 5：入库/导入任务启动恢复，旧的 `pending/running` 任务会标记为 `interrupted`，前端展示“构建已中断”提示。
- 已完成 P1 Task 6：RAG runtime 与收藏夹入库逻辑抽到 service 层，主流程 router 不再 import legacy `knowledge.py`。
- 已完成 P1 Task 7：聊天前端已拆出消息列表、输入区、模型配置弹窗、联网搜索配置弹窗、Markdown 代码块、共享类型与流式聊天 hook。
- 本轮验证通过：`python -m pytest -q` 为 214 passed。
- 本轮验证通过：`cd frontend; npm test` 为 22 个测试文件、127 tests passed。
- 本轮验证通过：`cd frontend; npm run lint`。
- 本轮验证通过：`cd frontend; npm run build`。

2026-06-26：

- 已完成 P2 Route A 的对话历史第一版：新增 `/chat/conversations` 独立接口，历史按 `user_id` 硬隔离，`workspace_id`、`knowledge_base_id`、`folder_ids`、`bvids`、联网搜索状态作为管理和恢复上下文元数据。
- 已新增 `chat_conversations`、`chat_messages` 模型和 SQLite legacy table/index 兼容初始化。
- 前端已新增“最近对话”入口与“新对话”，可打开历史并恢复消息、提问范围、联网搜索开关和 provider；一次流式回答完成后保存完整消息快照。
- TDD 红灯已确认：新增 `ChatPanel.test.tsx` 历史用例在实现前因未加载历史、无入口、未保存快照而失败。
- 验证通过：`python -m pytest tests/test_chat_history.py -q` 为 5 passed。
- 验证通过：`cd frontend; npm test -- ChatPanel.test.tsx` 为 27 tests passed。
- 验证通过：`cd frontend; npm test` 为 23 个测试文件、133 tests passed。
- 验证通过：`cd frontend; npm run lint`。
- 验证通过：`cd frontend; npm run build`。
- 验证通过：`python -m pytest -q` 为 220 passed。
- 已完成 OAuth state 浏览器绑定加固：Google/WeChat/QQ 登录发起时写入 HttpOnly/SameSite=Lax 临时 nonce cookie，回调时要求 state 中 nonce 与同浏览器 cookie 匹配，成功登录后清理临时 cookie。
- 已修正 legacy B 站会话注释：当前新写入的敏感 Cookie 字段已加密持久化，并兼容旧明文读取。
- 验证通过：`python -m pytest tests/test_system_auth.py -q` 为 30 passed。
- 提交前全量验证通过：`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-before-commit.ps1 -Format` 为后端 269 passed、前端 29 files / 149 tests passed、lint/build passed。
- 提交前全量验证通过：`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-before-commit.ps1` 为后端 269 passed、前端 29 files / 149 tests passed、lint/build passed。
- 已继续降低 ChatPanel 大文件风险：将空状态与提示词 chips 拆到 `frontend/components/chat/ChatEmptyState.tsx`，主面板只保留组合与回调传递。
- TDD 红灯已确认：`python -m pytest tests/test_frontend_structure.py -q` 在实现前因缺少 `ChatEmptyState.tsx` 失败。
- 验证通过：`python -m pytest tests/test_frontend_structure.py -q` 为 3 passed。
- 验证通过：`cd frontend; npm test -- ChatPanel.test.tsx` 为 28 tests passed。
- 已加固邮箱验证码 IP 限流：新增数据库持久化窗口计数，清空进程内 `_ip_rate_limit` 后仍会按同一 IP 拒绝第 4 次发送。
- TDD 红灯已确认：`python -m pytest tests/test_system_auth.py -q -k "rate_limit_survives_empty_memory_cache"` 在实现前因清空内存后第 4 次仍返回 200 失败。
- 验证通过：`python -m pytest tests/test_system_auth.py -q` 为 31 passed。
- 已加固内容源绑定二维码 pending state：`/source-bindings/bilibili/qrcode` 写入数据库短期状态，清空 legacy `login_sessions` 后仍可轮询等待状态，确认成功后清理 pending state。
- TDD 红灯已确认：`python -m pytest tests/test_source_bindings.py -q -k "qrcode_poll_survives_empty_memory_cache"` 在实现前因清空内存后轮询返回 404 失败。
- 验证通过：`python -m pytest tests/test_source_bindings.py -q` 为 6 passed。
- 已加固 legacy B 站二维码 pending state：`/auth/qrcode` 写入数据库短期状态，轮询前校验 pending 存在且 purpose 匹配，避免跨 worker 误报过期，也避免误用内容源绑定二维码状态；同时修复二维码生成/确认异常路径下 BilibiliService 未关闭的资源泄漏风险。
- TDD 红灯已确认：`python -m pytest tests/test_auth_legacy.py -q` 在实现前因未落库 pending、未知二维码继续打上游而失败；补充隔离红灯确认 source binding pending 不能进入 legacy poll。
- 验证通过：`python -m pytest tests/test_auth_legacy.py tests/test_source_bindings.py tests/test_system_auth.py -q` 为 46 passed。
- 已收口 scoped 收藏夹同步旧向量删除失败风险：删除旧向量失败时不再继续 `add_video_content`，避免向量库不可写或锁定时追加重复向量；FavoriteVideo 记录仍保留以维持同步状态。
- TDD 红灯已确认：`python -m pytest tests/test_knowledge_base_scoping.py -q -k "old_vector_delete_fails"` 在实现前因删除失败后仍调用 `add_video_content` 失败。
- 验证通过：`python -m pytest tests/test_folder_ingestion.py tests/test_knowledge_base_scoping.py -q` 为 63 passed。
- 已收口知识库删除时的孤儿向量风险：`/knowledge-bases/{id}` 删除前必须先完成当前 workspace + knowledge_base 范围的向量清理；向量清理失败时返回 `vector_cleanup_failed`，保留知识库记录供用户重试。
- TDD 红灯已确认：`python -m pytest tests/test_knowledge_scope.py -q -k "delete_knowledge_base"` 在实现前因旧逻辑仍返回 200 并删除记录失败。
- 已收口验证码日志泄露风险：SMTP 未配置时不再把验证码明文写入 warning 日志，避免生产缺配置场景扩大验证码暴露面。
- TDD 红灯已确认：`python -m pytest tests/test_email_service.py -q` 在实现前因 logger 参数包含验证码失败。
- 已收口 Google OAuth 异常处理裸 traceback 输出：统一改为 `logger.exception`，避免绕过日志系统直接打印堆栈。
- TDD 红灯已确认：`python -m pytest tests/test_service_boundaries.py -q -k tracebacks` 在实现前因 `traceback.print_exc()` 存在失败。
- 已收口 OAuth 网络异常泄露密钥风险：WeChat/QQ/Google 网络异常不再把底层 httpx 异常文本写入响应或 error 日志，避免异常文本携带 provider secret 时泄露。
- TDD 红灯已确认：`python -m pytest tests/test_system_auth.py -q -k "oauth_network_errors"` 在实现前因网络异常分支 500 或可能回显 secret 失败。

2026-06-27：

- 已收口收藏夹端点异常路径资源泄漏：`get_favorite_videos`、`get_all_favorite_videos`、`organize_preview`、`organize_execute`、`clean_invalid_resources` 均改为 `finally` 关闭 BilibiliService。
- TDD 红灯已确认：`python -m pytest tests/test_favorites_router.py tests/test_readme_links.py -q` 在实现前 5 个收藏夹关闭断言和中文 BAT 存在性检查失败。
- 已补齐 README/CLAUDE.md 提到的根目录中文 BAT 包装器：`安装依赖.bat`、`启动.bat`、`停止.bat`、`状态.bat`、`日志.bat`，统一委托到 `scripts\dev.ps1`。
- 已拆分 SQLite legacy 迁移入口：`_ensure_sqlite_legacy_columns` 只保留 SQLite 判定和 `run_sync` 调度，列补齐、video_cache 重建、索引创建、scoped 缓存克隆均拆到模块级 helpers。
- 结构红灯已确认：`python -m pytest tests/test_service_boundaries.py -q -k database_legacy` 在实现前因 `_ensure_sqlite_legacy_columns` 内仍存在 10 个嵌套 helper 失败。
- 验证通过：`python -m pytest tests/test_database_migration.py tests/test_service_boundaries.py tests/test_favorites_router.py tests/test_readme_links.py -q` 为 16 passed。
- 已补充 ASR 服务轻量单元测试：覆盖转写 JSON 中 `text`/`sentences`/顶层 `text` 提取、DashScope base URL 拼接、Recognition 输入格式选择，以及 `_transcribe_sync_with_model` 异常后恢复默认模型。
- 已在 worktree 分支 `maintenance/medium-low-risk-refactor` 继续处理前端中低优先级结构风险：提取 `AuthDemoPreview` 与 `useAuthDemoPreview`，避免登录页继续承载演示动画编排细节。
- 已提取 `VideoPlayerPortal`，`SourcesPanel` 不再直接持有 `createPortal` 和 B 站 iframe 拼装逻辑，并新增播放弹窗行为测试。
- 已提取 `useWorkspaceState`，`page.tsx` 不再直接持有移动端侧栏初始状态、宽度持久化和对话历史请求状态。
- TDD 红灯已确认：`python -m pytest tests/test_frontend_structure.py -q -k auth_demo`、`-k sources_video_player`、`-k workspace_state` 在实现前分别因目标文件/导入不存在失败。
- 定向验证通过：`python -m pytest tests/test_frontend_structure.py -q -k auth_demo`、`-k sources_video_player`、`-k workspace_state` 均已转绿。
- 定向验证通过：`cd frontend; npm test -- AuthPage.test.tsx` 为 1 file / 7 tests passed；`npm test -- SourcesPanel.test.tsx` 为 1 file / 5 tests passed；`npm test -- page.test.tsx` 为 2 files / 13 tests passed。
- 对抗复核发现前端依赖审计存在既有风险：`next@16.1.4`、`undici`、`@babel/core`、`js-yaml`、`brace-expansion` 等触发 `npm audit --audit-level=moderate`；已在 worktree 中升级 `next` / `eslint-config-next` 到 `16.2.9`，并用 `overrides.postcss=8.5.15` 覆盖 Next 内部旧 PostCSS，复核 `npm audit --audit-level=moderate` 为 0 vulnerabilities。
- TDD 红灯已确认：`cd frontend; npm test -- SourcesPanel.test.tsx` 在实现前因播放器关闭按钮缺少 `type="button"` 失败；已补齐 `VideoPlayerPortal` 关闭按钮类型，复测 `SourcesPanel.test.tsx` 为 1 file / 5 tests passed。
- main 合并后用同步后的 `node_modules` 复验时发现旧 `.next/dev/types` 会参与 Next 16.2.9 build 类型检查并失败；已新增 `frontend/scripts/clean-next-dev-types.mjs` 并接入 `prebuild`，在生产构建前清理 dev type cache，结构测试覆盖该边界。
- 本轮范围已明确为大版本前维护和架构优化，暂不进入学习复盘、导入源等产品功能；已在 `maintenance/pre-release-architecture` worktree 中启动结构优化。
- 已将 `SourcesPanel` 的时间格式化、收藏夹入库状态和入库按钮文案等纯逻辑抽到 `frontend/components/sources/sourcesPanelLogic.ts`，并新增纯单元测试与结构测试，降低资料面板继续膨胀的风险。
- 定向验证通过：`python -m pytest tests/test_frontend_structure.py -q -k sources_panel_pure_logic` 为 1 passed；`cd frontend; npm test -- sourcesPanelLogic.test.ts SourcesPanel.test.tsx` 为 2 files / 8 tests passed。
- 已将 `chat.py` 中的 LLM provider 元数据、provider env 映射、thinking 配置解析、`.env.local` 写入和联网搜索配置归一化抽到 `app/services/chat_config.py`，路由层只保留 HTTP 权限、请求编排和运行时聊天逻辑。
- 结构红灯已确认：`python -m pytest tests/test_service_boundaries.py -q -k chat_router_delegates_configuration_boundaries_to_service` 在实现前因缺少 `app/services/chat_config.py` 失败；实现后已转绿。
- 定向验证通过：`python -m pytest tests/test_chat_config_permissions.py tests/test_chat_user_credentials.py -q` 为 14 passed；`python -m pytest tests/test_chat_thinking.py -q` 为 26 passed；`python -m pytest tests/test_service_boundaries.py -q` 为 7 passed。
- 已开始拆分 `globals.css` 样式边界：登录页 auth 样式已抽到 `frontend/app/styles/auth.css` 并由 `globals.css` 顶部导入；全局 `a` 样式保留在原文件，modal/source/sidebar 样式继续作为后续维护目标。
- CSS 结构红灯已确认：`python -m pytest tests/test_frontend_structure.py -q -k global_styles_delegate_auth_styles_to_feature_file` 在实现前因缺少 `frontend/app/styles/auth.css` 失败；实现后已转绿。
- 定向验证通过：`cd frontend; npm test -- AuthPage.test.tsx` 为 1 file / 7 tests passed；`npm test -- api-accounts-layout.test.ts mobile-chat-layout.test.ts source-status-theme.test.ts` 为 3 files / 21 tests passed；`npm run build` passed。
- 已继续拆分 `globals.css` 样式边界：通用 `.modal-backdrop` / `.modal-card` / `.modal-title` / `.modal-subtitle` 与 provider 配置弹窗样式已抽到 `frontend/app/styles/modals.css`；业务弹窗样式仍分批保留，避免一次性移动过大。
- 已新增 `frontend/app/testStyles.ts`，前端样式结构测试会展开 `globals.css` 的本地 `@import`，后续继续拆样式时仍验证最终样式集合而不是单一文件。
- modal CSS 定向验证通过：`python -m pytest tests/test_frontend_structure.py -q -k "global_styles_delegate_modal_shell_styles_to_feature_file or global_styles_delegate_auth_styles_to_feature_file"` 为 2 passed；`cd frontend; npm test -- api-accounts-layout.test.ts mobile-chat-layout.test.ts source-status-theme.test.ts ChatPanel.test.tsx` 为 4 files / 49 tests passed；`npm run build` passed。
- Round 2 继续按架构维护范围拆分 `globals.css`：workspace/app shell、用户菜单/管理员/API key 面板、资料/收藏夹视频面板、知识库侧栏样式分别迁入 `frontend/app/styles/workspace.css`、`account-panels.css`、`sources.css`、`knowledge-sidebar.css`，选择器与属性保持不改名。
- 样式边界红灯已确认：`python -m pytest tests/test_frontend_structure.py -q -k "workspace_styles or account_panel_styles or sources_styles or knowledge_sidebar_styles"` 在实现前因目标样式文件不存在失败；迁移后已转绿。
- Round 2 定向验证通过：`python -m pytest tests/test_frontend_structure.py -q` 为 14 passed；`cd frontend; npm test -- page.test.tsx UserMenu.test.tsx ApiAccountsPanel.test.tsx` 为 4 files / 20 tests passed；`cd frontend; npm test -- SourcesPanel.test.tsx mobile-chat-layout.test.ts source-status-theme.test.ts api-accounts-layout.test.ts` 为 4 files / 26 tests passed。
- Round 3 继续按架构维护范围拆分 `globals.css`：聊天消息/Markdown/代码块/thinking/联网搜索展示样式迁入 `frontend/app/styles/chat.css`，composer/scope picker/model selector/输入按钮/status pill 控件样式迁入 `frontend/app/styles/chat-controls.css`，导入与整理弹窗样式迁入 `frontend/app/styles/import-organize.css`，选择器与属性保持不改名。
- 样式边界红灯已确认：`python -m pytest tests/test_frontend_structure.py -q -k "chat_styles or chat_control_styles or import_organize_styles"` 在实现前因目标样式文件不存在失败；迁移后已转绿。
- Round 3 定向验证通过：`python -m pytest tests/test_frontend_structure.py -q` 为 17 passed；`cd frontend; npm test -- ChatPanel.test.tsx mobile-chat-layout.test.ts source-status-theme.test.ts ImportModal.test.tsx SourcesPanel.test.tsx api-accounts-layout.test.ts` 为 6 files / 57 tests passed；格式化后复验 `ChatPanel.test.tsx mobile-chat-layout.test.ts source-status-theme.test.ts ImportModal.test.tsx` 为 4 files / 49 tests passed。
- 对抗检查结论：本轮已完成资料面板纯逻辑、chat 配置边界、auth 样式、通用 modal/provider 配置样式、workspace/account/sources/knowledge 样式分层、chat/composer/import/organize 样式分层和样式测试复用；`globals.css` 目前主要保留主题变量、基础 reset 与全局基础规则，残余 demo/local/glass 相关样式已迁入 feature 样式文件。学习复盘、导入源等产品功能仍暂缓。
- 已继续处理大版本前维护项 1/5/6：`globals.css` 残余 demo、empty hero、glass action、user message action 与 `fadeUp` 已迁入 feature 样式文件；`app/services/ingestion_tasks.py` 承接入库任务创建与构建状态展示映射；生产部署文档已要求网关限流或反向代理限流覆盖 `POST /system-auth/send-code`，按真实客户端 IP 做 per-IP 限制并保留可信 `X-Forwarded-For`。
- 四个维护问题收尾：入库任务创建/更新/状态展示由 service 统一承接；`test_folder_ingestion.py` 承接 scoped folder-sync 回归测试，避免大测试继续从 legacy router 导入；README、APK 文档、功能大纲和本执行方案已补齐当前维护边界说明。
- 继续低风险拆分测试架构：`tests/test_knowledge_base_web_search.py` 承接知识库联网搜索 prompt/query/context/fetch helper 测试，`tests/test_knowledge_base_scoping.py` 继续聚焦 scoped API 流程；结构测试防止这些 helper 回流到最大测试文件。
- 继续拆分知识库测试边界：`tests/test_knowledge_base_web_search_streaming.py` 承接联网搜索流式响应、progress/heartbeat 和流式来源回填测试；`tests/test_service_boundaries.py` 新增结构守卫，防止这些流式测试回流到 `tests/test_knowledge_base_scoping.py`。
- 继续拆分知识库联网搜索 API 测试：`tests/test_knowledge_base_web_search_api.py` 承接非流式 `/knowledge-bases/{id}/chat` 联网搜索工具链、fetch page、默认关闭与强制启用场景；`tests/test_knowledge_base_scoping.py` 降至约 1800 行，后续继续保留 scoped stats/search/chat/scope/build 主流程测试。
- 继续拆分剩余联网搜索测试：`tests/test_knowledge_base_web_search_tool_run.py` 承接 `_prepare_web_search_tool_run` 初始上下文、provider/API key、诊断和工具结果追加测试；`tests/test_knowledge_base_web_search_fallback.py` 承接无向量命中、DB fallback 隔离和外部来源回填测试；`tests/test_knowledge_base_scoping.py` 降至约 1000 行并保留纯 scoped 主流程。
- 继续收束知识库作用域测试：`tests/test_knowledge_base_streaming.py` 承接普通 `/chat/stream` 所有权、thinking 与 sources trailer 测试；`tests/test_knowledge_base_scope_build.py` 承接 folder/video scope、scope-options、build 与 status polling 测试；`tests/test_knowledge_base_scoping.py` 降至约 270 行，只保留 stats/search/chat 的基础 scoped 隔离测试与共享测试 helper。
- 继续拆分运行时代码边界：`app/services/llm_tool_calls.py` 承接 LLM tool-call 消息清洗、参数归一、DSML 文本工具调用解析、工具结果追加和 thinking/answer 提取；`app/routers/chat.py` 保留兼容 alias 与 LLM 调用编排，行数降至约 1500 行，并为后续拆 `knowledge_bases.py` 的联网搜索 orchestration 降低反向依赖风险。
- 继续拆分知识库运行时代码边界：`app/services/knowledge_web_search.py` 承接联网搜索 query 构造、上下文格式化、结果去重、状态载荷、诊断合并、no-results 系统消息和工具 schema；`app/routers/knowledge_bases.py` 保留兼容 alias 与异步工具/路由编排，行数降至约 1300 行。
- 继续拆分资料面板状态边界：`frontend/components/sources/useSourcesPanelData.ts` 承接收藏夹列表、scoped 入库状态和刷新；`useSourcesKnowledgeBuild.ts` 承接知识库 build 请求、状态轮询和完成通知；`useSourcesPanelActions.ts` 承接视频展开、重命名和整理预览动作；`SourcesPanel.tsx` 降至约 512 行，主要保留选择状态、播放弹窗状态与 JSX 组合。
- 结构红灯已确认：`python -m pytest tests/test_frontend_structure.py -q -k sources_panel_data_loading_is_extracted`、`-k sources_panel_knowledge_build_is_extracted`、`-k sources_panel_actions_are_extracted` 在实现前分别因目标 hook 不存在失败；实现后已转绿。
- 本轮验证通过：`python -m pytest tests/test_frontend_structure.py -q` 为 33 passed；`cd frontend; npm test -- SourcesPanel.test.tsx sourcesPanelLogic.test.ts` 为 2 files / 8 tests passed；`npm run lint` 与 `npm run build` passed；提交前完整验证 `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-before-commit.ps1 -Format` 与无 `-Format` 均通过，后端 373 passed、前端 35 files / 153 tests passed。
- 继续拆分聊天控件样式边界：`chat-controls.css` 由 1197 行收缩为聚合入口，composer、scope picker、model controls、通用控件 primitive 与响应式覆盖分别迁入 `chat-composer-controls.css`、`chat-scope-picker.css`、`chat-model-controls.css`、`control-primitives.css`、`chat-controls-responsive.css`；选择器与样式内容保持原样，展开后的 CSS 与拆分前一致。
- 结构红灯已确认：`python -m pytest tests/test_frontend_structure.py -q -k chat_control_styles_delegate_to_focused_files` 在实现前因目标样式子文件不存在失败；实现后已转绿。
- 定向验证通过：`python -m pytest tests/test_frontend_structure.py -q` 为 34 passed；`cd frontend; npm test -- ChatPanel.test.tsx ChatPanel.config.test.tsx ChatScopePicker.test.tsx mobile-chat-layout.test.ts source-status-theme.test.ts` 为 5 files / 41 tests passed；`npm run lint` 与 `npm run build` passed。
- 继续拆分 workspace 样式边界：`workspace.css` 由 985 行收缩为聚合入口，app shell、sidebar shell/panel、history/notes sidebar、embedded chat 和移动端响应式覆盖分别迁入 `workspace-shell.css`、`workspace-sidebar.css`、`workspace-history.css`、`workspace-chat-embedded.css`、`workspace-responsive-base.css`、`workspace-mobile-fullscreen.css`；选择器与样式内容保持原样，展开后仅文件拼接空行不同。
- 结构红灯已确认：`python -m pytest tests/test_frontend_structure.py -q -k workspace_styles_delegate_to_focused_files` 在实现前因目标 workspace 样式子文件不存在失败；实现后已转绿。
- 定向验证通过：`python -m pytest tests/test_frontend_structure.py -q -k "workspace_styles_delegate_to_focused_files or global_styles_delegate_workspace_styles_to_feature_file"` 为 2 passed；`cd frontend; npm test -- page.test.tsx mobile-chat-layout.test.ts source-status-theme.test.ts ChatPanel.history.test.tsx` 为 5 files / 33 tests passed。
- 继续拆分知识库流式聊天运行时代码边界：`app/services/knowledge_base_chat_stream.py` 承接 scoped 文档加载、LLM 凭证解析、联网搜索准备、thinking/answer 流输出、sources trailer 和 usage 记录；`app/routers/knowledge_bases.py` 中 `/knowledge-bases/{id}/chat/stream` 仅保留依赖注入、服务委派与 `StreamingResponse` 返回。
- 拆分过程中修复流式响应资源生命周期风险：数据库读取和凭证/API key 解析在返回流式 iterator 前完成，避免把请求 DB session 的读取阶段延后到响应迭代期，从而规避 Windows SQLite 临时库清理时的连接锁定。
- 定向验证通过：`python -m pytest tests/test_knowledge_base_scope_build.py -q -k scoped_chat_stream_uses_same_resolved_scope` 为 1 passed；`python -m pytest tests/test_knowledge_base_streaming.py tests/knowledge_base_web_search_streaming -q` 为 14 passed；主线合并后 `python -m pytest tests/service_boundaries/test_knowledge_base_router.py tests/service_boundaries/test_auth_database_ingestion.py -q` 为 28 passed。
- 提交前完整验证通过：`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-before-commit.ps1 -Format`，后端 419 passed、前端 35 files / 153 tests passed，frontend lint 与 Next build 均通过。
- 继续拆分 legacy chat 运行时代码边界：`app/services/chat_runtime.py` 承接 `/chat/ask` 与 `/chat/ask/stream` 的 LLM 配置解析、消息准备后处理、模型调用、连接异常降级、thinking delta 与 sources trailer 输出；`app/routers/chat.py` 对这两个端点只保留旧接口禁用校验、空问题校验、服务委派与响应封装。
- 结构红灯已确认：`python -m pytest tests/service_boundaries/test_chat_router.py -q -k legacy_ask_runtime` 在实现前因缺少 `app/services/chat_runtime.py` 失败；实现后已转绿。
- 已新增 `tests/test_chat_runtime.py` 覆盖非流式成功、非流式连接异常降级、流式 thinking/sources trailer、流式连接异常降级，避免新服务只有结构测试保护。
- 验证通过：`python -m pytest tests/test_chat_runtime.py tests/test_chat_completion.py tests/test_chat_routing.py tests/test_chat_thinking.py tests/test_chat_config_permissions.py tests/test_chat_user_credentials.py tests/service_boundaries/test_chat_router.py -q` 为 41 passed；提交前完整验证通过，后端 424 passed、前端 35 files / 153 tests passed，frontend lint 与 Next build 均通过。
- 继续拆分 chat 配置响应边界：`app/services/chat_config.py` 承接 `/chat/llm/config` 的用户 API 账号查询、官方/个人来源选择、provider 展示 payload 组装和 thinking template 注入；`app/routers/chat.py` 仅保留端点依赖注入与 `_llm_config_response` alias。
- 结构红灯已确认：`python -m pytest tests/service_boundaries/test_chat_router.py -q -k configuration_boundaries` 在实现前因缺少 `llm_config_response` / `current_user_llm_source` 失败；实现后已转绿。
- 已新增 `tests/test_chat_config.py` 覆盖当前用户 LLM source 选择逻辑，HTTP 层继续由 `tests/test_chat_config_permissions.py` 与 `tests/test_chat_user_credentials.py` 覆盖 provider payload 行为。
- 验证通过：`python -m pytest tests/test_chat_config.py tests/test_chat_config_permissions.py tests/test_chat_user_credentials.py tests/service_boundaries/test_chat_router.py -q` 为 25 passed；提交前完整验证通过，后端 426 passed、前端 35 files / 153 tests passed，frontend lint 与 Next build 均通过。
- 继续拆分 source binding 凭据服务工厂边界：`app/services/source_binding_services.py` 承接绑定归属校验、最新有效 `SourceCredential` 查询、凭据解密和 `BilibiliService` 构造；`app/routers/source_bindings.py` 不再直接 `select(SourceCredential)` 或解密凭据。
- 结构红灯已确认：`python -m pytest tests/service_boundaries/test_source_binding_router.py -q -k authenticated_service_factory` 在实现前因缺少 `app/services/source_binding_services.py` 失败；实现后已转绿。
- 已新增 `tests/test_source_binding_services.py` 覆盖凭据服务构造成功、绑定缺失、凭据缺失和解密失败分支，避免该安全边界只有路由行为测试覆盖。
- 验证通过：`python -m pytest tests/test_source_binding_services.py tests/test_source_bindings.py tests/service_boundaries/test_source_binding_router.py -q` 为 16 passed；提交前完整验证通过，后端 431 passed、前端 35 files / 153 tests passed，frontend lint 与 Next build 均通过。
- 继续拆分 source binding 二维码绑定流程：`app/services/source_binding_services.py` 承接 B 站绑定二维码生成、pending state 持久化、二维码轮询、确认后用户信息读取、`SourceBinding` / `SourceCredential` 创建与 pending 清理；`app/routers/source_bindings.py` 对二维码端点只保留依赖注入和服务委派。
- 结构红灯已确认：`python -m pytest tests/service_boundaries/test_source_binding_router.py -q -k qr_flow` 在实现前因服务层缺少二维码流程函数失败；实现后已转绿。
- 已扩展 `tests/test_source_binding_services.py` 覆盖二维码生成成功、生成失败关闭 service、轮询确认写入绑定和凭据、内存 pending 丢失时使用持久化 pending state 的路径。
- 验证通过：`python -m pytest tests/test_source_binding_services.py tests/test_source_bindings.py tests/service_boundaries/test_source_binding_router.py -q` 为 21 passed；提交前完整验证通过，后端 436 passed、前端 35 files / 153 tests passed，frontend lint 与 Next build 均通过。
- 继续拆分 imports 导入任务运行时边界：`app/services/import_tasks.py` 承接 B 站视频导入、本地视频导入、`VideoCache` / `FavoriteFolder` / `FavoriteVideo` 持久化、旧向量清理和本地上传清理；`app/routers/imports.py` 保留上传校验、知识库归属校验、入库任务创建、`BackgroundTasks` 委派和兼容 alias。
- 结构红灯已确认：`python -m pytest tests/service_boundaries/test_auth_database_ingestion.py -q -k import_task_runtime` 在实现前因缺少 `app/services/import_tasks.py` 失败；实现后已转绿。
- 兼容回归已保留：router 私有 `_run_*` / `_cleanup_*` 入口改为薄包装，旧测试和潜在内部 monkeypatch 仍能注入 `ASRService`、`get_rag_service`、`logger.warning`，但数据库查询、向量写入和内容入库重逻辑不再回流到 router。
- 验证通过：`python -m pytest tests/test_imports.py tests/test_ingestion_tasks.py tests/service_boundaries/test_auth_database_ingestion.py -q` 为 27 passed；提交前完整验证通过，后端 437 passed、前端 35 files / 153 tests passed，frontend lint 与 Next build 均通过。
- 继续拆分收藏夹入库服务边界：`app/services/folder_ingestion_records.py` 承接收藏夹记录、视频缓存、scoped 查询和向量删除入口；`app/services/folder_ingestion_content.py` 承接视频信息提取、内容来源优先级、缓存刷新判断和缓存转 `VideoContent`；`app/services/folder_ingestion.py` 继续保留同步编排与兼容私有 alias。
- 结构红灯已确认：`python -m pytest tests/service_boundaries/test_knowledge_base_router.py -q -k folder_ingestion_delegates_records` 在实现前因缺少新 records/content helper 模块失败；实现后已转绿。
- 已扩展 `tests/test_folder_ingestion.py` 覆盖 content helper 的来源优先级/缓存转换，以及 records helper 的 scoped cache 查询，避免新模块只有结构测试保护。
- 验证通过：`python -m pytest tests/test_folder_ingestion.py tests/test_knowledge_base_scoping.py tests/test_knowledge_base_scope_build.py tests/service_boundaries/test_knowledge_base_router.py -q` 为 41 passed；提交前完整验证通过，后端 440 passed、前端 35 files / 153 tests passed，frontend lint 与 Next build 均通过。
- 继续拆分内容抓取服务边界：`app/services/content_summary.py` 承接 B 站 AI 摘要响应解析、分段提纲归一化和摘要内容格式化；`app/services/content_fetcher.py` 继续保留内容获取降级编排、字幕和 ASR 调度。
- 结构红灯已确认：`python -m pytest tests/service_boundaries/test_auth_database_ingestion.py -q -k content_fetcher_delegates_ai_summary` 在实现前因缺少 `app/services/content_summary.py` 失败；实现后已转绿。
- 已扩展 `tests/test_content_fetcher.py` 覆盖摘要 helper 的 payload 解析、格式化输出，以及不可用/空摘要拒绝分支。
- 验证通过：`python -m pytest tests/test_content_fetcher.py tests/test_folder_ingestion.py tests/test_imports.py tests/test_asr_config.py tests/service_boundaries/test_auth_database_ingestion.py -q` 为 41 passed；提交前完整验证通过，后端 443 passed、前端 35 files / 153 tests passed，frontend lint 与 Next build 均通过。
- 继续拆分 Bilibili 外部平台服务边界：`app/services/bilibili_cookies.py` 承接 cookie 别名归一化与 service factory，`app/services/bilibili_responses.py` 承接 B 站 JSON 响应解析；`app/services/bilibili.py` 保留网络调用编排、`BilibiliService` 和旧导入兼容入口，避免后续导入源扩展继续堆进单一大文件。
- TDD 红灯已确认：`python -m pytest tests/service_boundaries/test_auth_database_ingestion.py -q -k bilibili_service_delegates_cookie_and_response_helpers` 在实现前因目标 helper 模块不存在失败；`python -m pytest tests/test_bilibili_helpers.py -q` 在实现前因 `app.services.bilibili_cookies` 不存在失败。
- 验证通过：定向回归 `python -m pytest tests/test_bilibili_helpers.py tests/test_bilibili_service.py tests/test_source_binding_services.py tests/test_source_bindings.py tests/test_favorites_router.py tests/test_content_fetcher.py tests/service_boundaries/test_auth_database_ingestion.py -q` 为 48 passed；提交前完整验证 `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-before-commit.ps1 -Format` 为后端 446 passed、前端 35 files / 153 tests passed、frontend lint 与 Next build passed；主线合并后同一组定向回归仍为 48 passed。
- 继续拆分 RAG 向量服务边界：`app/services/rag_documents.py` 承接 `VideoContent` 正文/提纲拼接与 `Document` metadata 构造，`app/services/rag_filters.py` 承接 workspace / knowledge_base / bvid 的 Chroma filter 构造；`app/services/rag.py` 保留向量库写入、检索、删除和 LLM 编排入口，降低后续学习复盘、多导入源和多内容类型入库时的耦合。
- TDD 红灯已确认：`python -m pytest tests/test_rag_service.py -q -k "rag_document_helpers or rag_filter_helpers"` 在实现前因 `app.services.rag_documents` 不存在失败；`python -m pytest tests/service_boundaries/test_knowledge_base_router.py -q -k rag_service_delegates_document_and_filter_helpers_to_services` 在实现前因 helper 模块不存在失败。
- 验证通过：定向回归 `python -m pytest tests/test_rag_service.py tests/test_knowledge_scope.py tests/test_security_isolation.py tests/test_knowledge_base_scoping.py tests/test_knowledge_base_scope_build.py tests/test_folder_ingestion.py tests/test_imports.py tests/service_boundaries/test_knowledge_base_router.py -q` 为 71 passed；提交前完整验证 `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-before-commit.ps1 -Format` 为后端 449 passed、前端 35 files / 153 tests passed、frontend lint 与 Next build passed；主线合并后同一组定向回归仍为 71 passed。
- 继续拆分 `app/models.py` schema 边界：新增 `app/schemas/auth.py` 承接系统登录、注册、用户、管理员和工作区认证响应 schema，新增 `app/schemas/api_accounts.py` 承接用户 API 账户请求/响应 schema；`app/models.py` 保留 SQLAlchemy 模型、剩余 Pydantic schema 和旧导入兼容 re-export，避免一次性迁移所有调用点。
- TDD 红灯已确认：`python -m pytest tests/service_boundaries/test_model_boundaries.py -q` 在实现前因 `app/schemas/auth.py` 不存在失败；`python -m pytest tests/test_schema_compatibility.py -q` 在实现前因 `app.schemas` 不存在失败。
- 验证通过：定向回归 `python -m pytest tests/service_boundaries/test_model_boundaries.py tests/test_schema_compatibility.py tests/test_user_api_accounts.py tests/test_chat_config_permissions.py tests/test_chat_user_credentials.py tests/system_auth tests/test_source_bindings.py -q` 为 63 passed；提交前完整验证 `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-before-commit.ps1 -Format` 为后端 452 passed、前端 35 files / 153 tests passed、frontend lint 与 Next build passed；主线合并后同一组定向回归仍为 63 passed。
- 继续拆分 `app/models.py` knowledge-base schema 边界：新增 `app/schemas/knowledge_base.py` 承接 `KnowledgeBase*`、`KnowledgeScope*` 与 build/search/chat 请求响应 schema；`app/models.py` 继续 re-export 旧导入路径，知识库 router/service 的 schema import 已切到 `app.schemas.knowledge_base`，ORM 模型仍留在 `app.models`。
- TDD 红灯已确认：`python -m pytest tests/service_boundaries/test_model_boundaries.py -q` 在实现前因 `app/schemas/knowledge_base.py` 不存在失败；`python -m pytest tests/test_schema_compatibility.py -q` 在实现前因 `app.schemas.knowledge_base` 不存在失败。
- 验证通过：定向回归 `python -m pytest tests/service_boundaries/test_model_boundaries.py tests/test_schema_compatibility.py tests/knowledge_scope_filters tests/test_knowledge_scope.py tests/test_knowledge_base_scope_build.py tests/test_knowledge_base_scoping.py tests/test_knowledge_base_streaming.py tests/knowledge_base_web_search_streaming tests/chat_thinking/test_request_models.py -q` 为 66 passed；提交前完整验证 `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-before-commit.ps1 -Format` 为后端 454 passed、前端 35 files / 153 tests passed、frontend lint 与 Next build passed；主线合并后同一组定向回归仍为 66 passed。
- 继续拆分 `app/models.py` source/content schema 边界：新增 `app/schemas/content.py` 承接 `ContentSource`、`VideoInfo`、`VideoContent`、`FavoriteFolderInfo`，新增 `app/schemas/source_bindings.py` 承接 `SourceBindingResponse`、`QRCodeResponse`、`LoginStatusResponse`；内容抓取、RAG、导入、收藏夹、auth 与 source binding router/service 的 schema import 已切到新模块，`app.models` 继续 re-export 旧路径。
- TDD 红灯已确认：`python -m pytest tests/service_boundaries/test_model_boundaries.py -q` 在实现前因 `app/schemas/content.py` 不存在失败；`python -m pytest tests/test_schema_compatibility.py -q` 在实现前因 `app.schemas.content` 不存在失败。
- 验证通过：定向回归 `python -m pytest tests/service_boundaries/test_model_boundaries.py tests/test_schema_compatibility.py tests/test_source_bindings.py tests/test_source_binding_services.py tests/test_bilibili_helpers.py tests/test_content_fetcher.py tests/test_favorites_router.py tests/test_folder_ingestion.py tests/test_rag_service.py tests/test_imports.py -q` 为 57 passed；提交前完整验证 `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-before-commit.ps1 -Format` 为后端 457 passed、前端 35 files / 153 tests passed、frontend lint 与 Next build passed；主线合并后同一组定向回归仍为 57 passed。
- 继续拆分 `app/models.py` chat schema 边界：新增 `app/schemas/chat.py` 承接 `ChatRequest`、`ChatResponse`、`ChatHistory*` 与 `ChatConversation*` 请求/响应 schema；legacy chat、chat history、knowledge-base chat 相关 router/service 的 schema import 已切到新模块，`app.models` 继续 re-export 旧路径，`ChatConversation` / `ChatMessage` ORM 仍留在模型层。
- TDD 红灯已确认：`python -m pytest tests/service_boundaries/test_model_boundaries.py -q` 在实现前因 `app/schemas/chat.py` 不存在失败；`python -m pytest tests/test_schema_compatibility.py -q` 在实现前因 `app.schemas.chat` 不存在失败。
- 验证通过：定向回归 `python -m pytest tests/service_boundaries/test_model_boundaries.py tests/test_schema_compatibility.py tests/chat_thinking/test_request_models.py tests/test_chat_runtime.py tests/test_chat_completion.py tests/test_chat_routing.py tests/test_chat_thinking.py tests/test_chat_config_permissions.py tests/test_chat_user_credentials.py tests/service_boundaries/test_chat_router.py tests/test_chat_history.py tests/test_knowledge_base_streaming.py tests/test_knowledge_base_scoping.py -q` 为 68 passed；提交前完整验证 `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-before-commit.ps1 -Format` 为后端 459 passed、前端 35 files / 153 tests passed、frontend lint 与 Next build passed；主线合并后同一组定向回归仍为 68 passed。
- 继续拆分 chat 全局配置写入边界：`app/services/chat_config.py` 承接 `save_global_web_search_config`、`save_global_llm_provider_config` 与 `set_global_llm_provider`，统一处理 `.env.local` 写入 payload、settings 同步、provider 验证、thinking config 序列化和 RAG runtime reset；`app/routers/chat.py` 只保留管理员权限依赖、请求体和 service 委托。
- 兼容边界已保留：路由层继续暴露 `_write_env_values`、`_verify_provider_configuration`、`_resolve_llm_config`、thinking helper 等旧 alias，并作为依赖注入到 service，现有权限测试、安全隔离测试和 monkeypatch 场景不用改调用方式。
- TDD 红灯已确认：`python -m pytest tests/service_boundaries/test_chat_router.py -q -k global_config_writes` 在实现前因 service 缺少全局写配置函数失败；`python -m pytest tests/test_chat_config.py -q` 在实现前因无法导入新 service 函数失败。
- 验证通过：定向回归 `python -m pytest tests/test_chat_config.py tests/test_chat_config_permissions.py tests/test_chat_user_credentials.py tests/service_boundaries/test_chat_router.py tests/test_chat_runtime.py tests/chat_thinking/test_configuration.py tests/test_security_isolation.py -q` 为 48 passed；提交前完整验证 `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-before-commit.ps1 -Format` 为后端 462 passed、前端 35 files / 153 tests passed、frontend lint 与 Next build passed；主线合并后同一组定向回归仍为 48 passed。
- 继续拆分 chat 配置 env 持久化边界：新增 `app/services/chat_config_env.py` 承接 `SETTINGS_FIELD_BY_ENV`、`.env.local` 路径解析、env 文件读写、追加缺失键和 settings 同步；`app/services/chat_config.py` 只通过 import re-export 旧 `_env_file_path`、`_read_env_values`、`_write_env_values` 与映射，保持旧调用兼容。
- TDD 红灯已确认：`python -m pytest tests/test_chat_config_env.py tests/service_boundaries/test_chat_router.py -q -k "chat_config_env or env_persistence"` 在实现前因 `app.services.chat_config_env` 不存在与 helper 文件缺失失败；实现后已转绿。
- 验证通过：定向回归 `python -m pytest tests/test_chat_config_env.py tests/test_chat_config.py tests/test_chat_config_permissions.py tests/service_boundaries/test_chat_router.py tests/chat_thinking/test_configuration.py tests/test_security_isolation.py -q` 为 37 passed；提交前完整验证 `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-before-commit.ps1 -Format` 为后端 464 passed、前端 35 files / 153 tests passed、frontend lint 与 Next build passed；主线合并后同一组定向回归仍为 37 passed。
- 继续拆分 chat web-search 配置边界：新增 `app/services/chat_web_search_config.py` 承接联网搜索 provider/depth 归一化、配置响应 payload 和全局联网搜索配置保存；`app/services/chat_config.py` 继续 import re-export 旧 `_normalize_web_search_provider`、`_normalize_tavily_search_depth`、`_web_search_config_response` 与 `save_global_web_search_config`，路由和旧测试导入路径不变。
- TDD 红灯已确认：`python -m pytest tests/test_chat_web_search_config.py tests/service_boundaries/test_chat_router.py -q -k "web_search_config"` 在实现前因 `app.services.chat_web_search_config` 不存在与 helper 文件缺失失败；实现后已转绿。
- 验证通过：定向回归 `python -m pytest tests/test_chat_web_search_config.py tests/test_chat_config.py tests/test_chat_config_env.py tests/test_chat_config_permissions.py tests/service_boundaries/test_chat_router.py tests/chat_thinking/test_configuration.py tests/test_security_isolation.py -q` 为 40 passed；提交前完整验证 `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-before-commit.ps1 -Format` 为后端 467 passed、前端 35 files / 153 tests passed、frontend lint 与 Next build passed；主线合并后同一组定向回归仍为 40 passed。
- 继续拆分 chat provider catalog 边界：新增 `app/services/chat_provider_catalog.py` 承接 LLM provider 元数据、provider env 字段映射、thinking settings/template、provider 归一化、当前默认 provider fallback、全局 provider 配置解析与 thinking JSON 校验；`app/services/chat_config.py` 继续 import re-export 旧 `PROVIDER_*`、`SUPPORTED_LLM_PROVIDERS` 与 `_resolve_llm_config` 等 helper，路由、知识库 router 和旧 monkeypatch 路径保持不变。
- TDD 红灯已确认：`python -m pytest tests/test_chat_provider_catalog.py tests/service_boundaries/test_chat_router.py -q -k "provider_catalog"` 在实现前因 `app.services.chat_provider_catalog` 不存在与 helper 文件缺失失败；实现后已转绿。
- 验证通过：定向回归 `python -m pytest tests/test_chat_provider_catalog.py tests/test_chat_config.py tests/test_chat_config_permissions.py tests/chat_thinking/test_configuration.py tests/chat_thinking/test_tool_calls.py tests/chat_thinking/test_native_reasoning.py tests/service_boundaries/test_chat_router.py tests/test_security_isolation.py -q` 为 50 passed；提交前完整验证 `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-before-commit.ps1 -Format` 为后端 470 passed、前端 35 files / 153 tests passed、frontend lint 与 Next build passed；主线合并后同一组定向回归仍为 50 passed。
- 继续拆分 chat provider 配置写入边界：新增 `app/services/chat_provider_config.py` 承接全局 LLM provider 配置验证、thinking mode/template/custom JSON 解析、`.env.local` 写入 payload、RAG reset 与当前 provider 切换；`app/services/chat_config.py` 只保留 import re-export，继续兼容路由和旧测试导入路径。
- TDD 红灯已确认：`python -m pytest tests/test_chat_provider_config.py tests/service_boundaries/test_chat_router.py -q -k "provider_config"` 在实现前因 `app.services.chat_provider_config` 不存在与 helper 文件缺失失败；实现后已转绿。
- 验证通过：定向回归 `python -m pytest tests/test_chat_provider_config.py tests/test_chat_provider_catalog.py tests/test_chat_config.py tests/test_chat_config_permissions.py tests/chat_thinking/test_configuration.py tests/service_boundaries/test_chat_router.py tests/test_security_isolation.py -q` 为 43 passed；提交前完整验证 `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-before-commit.ps1 -Format` 为后端 473 passed、前端 35 files / 153 tests passed、frontend lint 与 Next build passed；主线合并后同一组定向回归仍为 43 passed。
- 继续拆分 chat LLM client factory 边界：新增 `app/services/llm_client.py` 承接 OpenAI 兼容客户端构造、API key 缺失校验和超时/重试参数；`app/routers/chat.py` 不再直接 import `OpenAI`，只保留 `_get_llm_client` 兼容 alias 供旧测试与内部 monkeypatch 路径继续注入。
- TDD 红灯已确认：`python -m pytest tests/test_llm_client.py -q` 在实现前因 `app.services.llm_client` 不存在失败；`python -m pytest tests/service_boundaries/test_chat_router.py -q -k llm_client_factory` 在实现前因 service 文件缺失失败；实现后均已转绿。
- 验证通过：定向回归 `python -m pytest tests/test_chat_user_credentials.py tests/test_chat_config.py tests/test_chat_config_permissions.py tests/service_boundaries/test_chat_router.py tests/test_security_isolation.py -q` 为 39 passed，知识库联网搜索相关回归 `python -m pytest tests/knowledge_base_web_search_api tests/knowledge_base_web_search_streaming tests/test_knowledge_base_web_search_tool_run.py tests/test_knowledge_base_web_search_fallback.py -q` 为 28 passed；提交前完整验证 `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-before-commit.ps1 -Format` 为后端 476 passed、前端 35 files / 153 tests passed、frontend lint 与 Next build passed；主线合并后 `python -m pytest tests/test_llm_client.py tests/test_chat_completion.py tests/test_chat_runtime.py tests/service_boundaries/test_chat_router.py -q` 为 28 passed。
- 继续拆分知识库 router 与 chat router 的共享 LLM runtime 依赖：`app/routers/knowledge_bases.py` 不再从 `app.routers.chat` 引入 `_resolve_llm_config`、LLM completion/stream/tool helper，而是直接依赖 `app.services.chat_completion`、`app.services.chat_provider_catalog`、`app.services.llm_client` 与 `app.services.llm_tool_calls`；知识库 router 保留 `_complete_llm_answer`、`_stream_llm_events`、`_prepare_llm_messages_with_tools` 等兼容 alias，测试注入点同步迁移到 `app.routers.knowledge_bases`。
- TDD 红灯已确认：`python -m pytest tests/service_boundaries/test_knowledge_base_router.py -q -k shared_llm_runtime` 在实现前因 `knowledge_bases.py` 仍 import `app.routers.chat` 失败；实现后已转绿。全量验证首轮暴露 `tests/test_chat_user_credentials.py` 仍 patch 旧 chat client 注入点，已按根因迁移到知识库 router 自身 alias。
- 验证通过：定向回归 `python -m pytest tests/test_chat_user_credentials.py tests/service_boundaries/test_knowledge_base_router.py tests/service_boundaries/test_chat_router.py tests/knowledge_base_web_search_api tests/knowledge_base_web_search_streaming tests/test_knowledge_base_web_search_tool_run.py tests/test_knowledge_base_web_search_fallback.py tests/test_knowledge_base_web_search.py tests/test_knowledge_base_streaming.py tests/test_knowledge_base_scoping.py -q` 为 84 passed；提交前完整验证 `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-before-commit.ps1 -Format` 为后端 477 passed、前端 35 files / 153 tests passed、frontend lint 与 Next build passed；主线合并后 `python -m pytest tests/test_chat_user_credentials.py tests/service_boundaries/test_knowledge_base_router.py tests/knowledge_base_web_search_api tests/knowledge_base_web_search_streaming tests/test_knowledge_base_web_search_tool_run.py tests/test_knowledge_base_web_search_fallback.py -q` 为 54 passed。
- 继续拆分 legacy B 站登录 session/cache 边界：新增 `app/services/legacy_bilibili_sessions.py` 承接 `login_sessions` 热缓存、二维码/登录 TTL、Cookie 加密/解密、数据库会话恢复和 `get_session`；`app/routers/auth.py` 只保留登录/轮询/logout HTTP 编排并 re-export 旧 helper，`source_bindings.py`、`favorites.py` 和 legacy `knowledge.py` 不再从 auth router 复用 session helper。
- TDD 红灯已确认：`python -m pytest tests/test_legacy_bilibili_sessions.py -q` 在实现前因 `app.services.legacy_bilibili_sessions` 不存在失败；`python -m pytest tests/service_boundaries/test_auth_database_ingestion.py -q -k legacy_bilibili_session` 在实现前因 service 文件缺失失败；实现后均已转绿。实现过程中新增 service 测试先暴露测试密钥/DB context 注入缺口，已按项目既有 fixture 模式修正。
- 验证通过：定向回归 `python -m pytest tests/test_legacy_bilibili_sessions.py tests/test_auth_legacy.py tests/test_source_bindings.py tests/test_source_binding_services.py tests/test_favorites_router.py tests/test_security_isolation.py tests/service_boundaries/test_auth_database_ingestion.py tests/service_boundaries/test_source_binding_router.py -q` 为 61 passed；提交前完整验证 `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-before-commit.ps1 -Format` 为后端 480 passed、前端 35 files / 153 tests passed、frontend lint 与 Next build passed；主线合并后同一组定向回归仍为 61 passed。
- 继续收口 chat router 与 system auth router 的管理员权限依赖：`app/routers/chat.py` 不再从 `app.routers.system_auth` import `_get_current_admin_user`，改为直接依赖 `app.services.system_auth_admin.get_current_admin_user` 并保留本地 alias，配置写入接口的权限行为不变。
- TDD 红灯已确认：`python -m pytest tests/service_boundaries/test_chat_router.py -q -k admin_service` 在实现前因 `chat.py` 仍 import `app.routers.system_auth` 失败；实现后已转绿。
- 验证通过：定向回归 `python -m pytest tests/test_chat_config_permissions.py tests/test_chat_config.py tests/test_security_isolation.py tests/service_boundaries/test_chat_router.py tests/service_boundaries/test_auth_database_ingestion.py -q` 为 49 passed；提交前完整验证 `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-before-commit.ps1 -Format` 为后端 481 passed、前端 35 files / 153 tests passed、frontend lint 与 Next build passed；主线合并后同一组定向回归仍为 49 passed。
- 继续拆分 source binding 收藏夹运行时边界：新增 `app/services/source_binding_favorites.py` 承接 B 站收藏夹列表、分页视频、全量有效视频、整理预览、移动执行和失效清理编排；`app/routers/source_bindings.py` 仅保留端点依赖注入、请求参数和 service 委派，行数由约 453 行降至约 308 行。
- TDD 红灯已确认：`python -m pytest tests/test_source_binding_favorites.py -q` 在实现前因 `app.services.source_binding_favorites` 不存在失败；`python -m pytest tests/service_boundaries/test_source_binding_router.py -q -k favorite_flows` 在实现前因 service 文件缺失失败，且路由仍包含收藏夹业务细节。
- 验证通过：定向回归 `python -m pytest tests/test_favorites_router.py tests/test_source_binding_favorites.py tests/test_source_bindings.py tests/test_source_binding_services.py tests/service_boundaries/test_source_binding_router.py tests/service_boundaries/test_auth_database_ingestion.py -q` 为 52 passed；提交前完整验证 `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-before-commit.ps1 -Format` 为后端 489 passed、前端 35 files / 153 tests passed、frontend lint 与 Next build passed；主线合并后 `python -m pytest tests/test_source_binding_favorites.py tests/test_source_bindings.py tests/test_source_binding_services.py tests/service_boundaries/test_source_binding_router.py -q` 为 29 passed。
- 继续收束 source binding 基础绑定边界：`app/services/source_binding_services.py` 承接绑定列表、撤销和 active 绑定归属校验，`get_bilibili_service_for_binding` 复用同一校验入口；`app/routers/source_bindings.py` 不再直接 `select(SourceBinding)`、`db.get(SourceBinding)` 或手写 user/workspace/status 判断，行数继续降至约 289 行。
- TDD 红灯已确认：`python -m pytest tests/test_source_binding_services.py -q -k "list_source_bindings or revoke_source_binding or ensure_active_source_binding"` 在实现前因无法导入新 service 函数失败；`python -m pytest tests/service_boundaries/test_source_binding_router.py -q -k binding_catalog` 在实现前因 service 函数缺失失败。
- 验证通过：定向回归 `python -m pytest tests/test_favorites_router.py tests/test_source_binding_favorites.py tests/test_source_bindings.py tests/test_source_binding_services.py tests/service_boundaries/test_source_binding_router.py tests/service_boundaries/test_auth_database_ingestion.py -q` 为 58 passed；提交前完整验证 `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-before-commit.ps1 -Format` 为后端 495 passed、前端 35 files / 153 tests passed、frontend lint 与 Next build passed；主线合并后同一组定向回归仍为 58 passed。
- 继续拆分内容抓取字幕边界：新增 `app/services/content_subtitles.py` 承接 B 站字幕列表提取、字幕 URL 提取、中文人工字幕优先选择、播放器接口与 view 字幕兜底下载流程；`app/services/content_fetcher.py` 的 `_try_subtitle` 缩为 service 委派，行数由约 554 行降至约 448 行。
- TDD 红灯已确认：`python -m pytest tests/test_content_fetcher.py -q -k "content_subtitle_helpers or try_bilibili_subtitle"` 在实现前因 `app.services.content_subtitles` 不存在失败；`python -m pytest tests/service_boundaries/test_auth_database_ingestion.py -q -k content_fetcher_delegates_subtitle_helpers` 在实现前因 service 文件缺失失败。
- 验证通过：定向回归 `python -m pytest tests/test_content_fetcher.py tests/test_folder_ingestion.py tests/test_imports.py tests/test_knowledge_base_scope_build.py tests/service_boundaries/test_auth_database_ingestion.py -q` 为 55 passed；提交前完整验证 `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-before-commit.ps1 -Format` 为后端 499 passed、前端 35 files / 153 tests passed、frontend lint 与 Next build passed；主线合并后同一组定向回归仍为 55 passed。
- 继续拆分内容抓取 ASR 音频边界：新增 `app/services/content_asr.py` 承接音频 URL 可达性探测、远程 Transcription、不可达时本地下载 Recognition 兜底和短文本过滤；`app/services/content_fetcher.py` 的 `_try_asr` 缩为 service 委派，行数继续降至约 357 行。
- TDD 红灯已确认：`python -m pytest tests/test_content_fetcher.py -q -k "try_bilibili_asr or try_asr_with_local_audio"` 在实现前因 `app.services.content_asr` 不存在失败；`python -m pytest tests/service_boundaries/test_auth_database_ingestion.py -q -k content_fetcher_delegates_asr_audio_helpers` 在实现前因 service 文件缺失失败。
- 验证通过：定向回归 `python -m pytest tests/test_content_fetcher.py tests/test_folder_ingestion.py tests/test_imports.py tests/test_knowledge_base_scope_build.py tests/service_boundaries/test_auth_database_ingestion.py -q` 为 60 passed；提交前完整验证 `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-before-commit.ps1 -Format` 为后端 504 passed、前端 35 files / 153 tests passed、frontend lint 与 Next build passed；主线合并后同一组定向回归仍为 60 passed。
- 继续拆分 Bilibili 媒体解析/下载边界：新增 `app/services/bilibili_media.py` 承接 playurl 音频 URL 选择、协议相对媒体 URL 规范化、字幕 JSON 转纯文本和带 Cookie/Referer 的音频流下载；`app/services/bilibili.py` 保留原 `get_audio_url`、`download_subtitle`、`download_audio_to_file` 兼容方法并委派到 helper，行数由约 535 行降至约 493 行。
- TDD 红灯已确认：`python -m pytest tests/test_bilibili_helpers.py -q` 在实现前因 `app.services.bilibili_media` 不存在失败；`python -m pytest tests/service_boundaries/test_auth_database_ingestion.py -q -k bilibili_service_delegates_media_helpers` 在实现前因 helper 文件不存在失败。
- 验证通过：定向回归 `python -m pytest tests/test_bilibili_service.py tests/test_bilibili_helpers.py tests/test_content_fetcher.py tests/test_folder_ingestion.py tests/test_favorites_router.py tests/test_source_binding_favorites.py tests/service_boundaries/test_auth_database_ingestion.py -q` 为 61 passed；提交前完整验证 `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-before-commit.ps1 -Format` 为后端 508 passed、前端 35 files / 153 tests passed、frontend lint 与 Next build passed；主线合并后同一组定向回归仍为 61 passed。
- 继续拆分知识库删除编排边界：`app/services/knowledge_base_delete.py` 承接 workspace-scoped 向量清理、失败时保留知识库记录并返回 `vector_cleanup_failed`、关联记录删除与事务提交；`app/routers/knowledge_bases.py` 的删除端点只保留依赖注入和 service 委派，行数由约 586 行降至约 570 行。
- TDD 红灯已确认：`python -m pytest tests/test_knowledge_base_delete.py -q` 在实现前因 `delete_knowledge_base` service 函数不存在失败；`python -m pytest tests/service_boundaries/test_knowledge_base_router.py -q -k record_deletion` 在实现前因路由仍直接执行删除编排失败。
- 验证通过：定向回归 `python -m pytest tests/test_knowledge_base_delete.py tests/test_knowledge_scope.py tests/test_knowledge_base_scoping.py tests/test_knowledge_base_scope_build.py tests/test_knowledge_base_streaming.py tests/service_boundaries/test_knowledge_base_router.py -q` 为 52 passed；提交前完整验证 `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-before-commit.ps1 -Format` 为后端 510 passed、前端 35 files / 153 tests passed、frontend lint 与 Next build passed；主线合并后同一组定向回归仍为 52 passed。
- 继续拆分知识库联网搜索兼容 shim 边界：新增 `app/services/knowledge_base_web_search_compat.py` 承接旧 `_execute_web_search_tool`、`_prepare_web_search_tool_run`、`_prepare_knowledge_base_web_search_with_heartbeats` 等 monkeypatch 兼容入口，`app/routers/knowledge_bases.py` 只保留 `__getattr__` 转发和当前 router module 注入，旧测试对 `app.routers.knowledge_bases.search_web` / `fetch_web_page` / `_prepare_knowledge_base_web_search` 的 patch 路径保持可用，路由行数由约 570 行降至约 442 行。
- TDD 红灯已确认：`python -m pytest tests/test_knowledge_base_web_search_compat.py -q` 在实现前因 `app.services.knowledge_base_web_search_compat` 不存在失败；`python -m pytest tests/service_boundaries/test_knowledge_base_router.py -q -k web_search_orchestration` 在实现前因兼容 service 文件不存在且路由仍声明 `_legacy_*` shim 失败。
- 验证通过：定向回归 `python -m pytest tests/test_knowledge_base_web_search_compat.py tests/service_boundaries/test_knowledge_base_router.py tests/knowledge_base_web_search_api tests/knowledge_base_web_search_streaming tests/test_knowledge_base_web_search.py tests/test_knowledge_base_web_search_tool_run.py tests/test_knowledge_base_web_search_fallback.py -q` 为 53 passed；提交前完整验证 `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify-before-commit.ps1 -Format` 为后端 512 passed、前端 35 files / 153 tests passed、frontend lint 与 Next build passed；主线合并后同一组定向回归仍为 53 passed。
- 继续拆分 RAG collection 操作边界：新增 `app/services/rag_collection_ops.py` 承接 Chroma collection 统计、清空、按视频删除、按知识库删除、scoped 视频向量存在性检查和 filter 组装；`app/services/rag.py` 不再直接调用 `_collection.delete/get/count`，只保留 RAGService 对外方法、日志和异常语义，行数由约 468 行降至约 450 行。
- TDD 红灯已确认：`python -m pytest tests/test_rag_collection_ops.py -q` 在实现前因 `app.services.rag_collection_ops` 不存在失败；`python -m pytest tests/service_boundaries/test_knowledge_base_router.py -q -k rag_service_delegates` 在实现前因 collection ops helper 缺失且 `rag.py` 仍直接调用 `_collection.delete/get/count` 失败。
- 验证通过：定向回归 `python -m pytest tests/test_rag_collection_ops.py tests/test_rag_service.py tests/test_knowledge_scope.py tests/test_knowledge_base_scoping.py tests/test_knowledge_base_scope_build.py tests/test_folder_ingestion.py tests/test_imports.py tests/service_boundaries/test_knowledge_base_router.py -q` 为 70 passed；提交前完整验证 `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify-before-commit.ps1 -Format` 为后端 516 passed、前端 35 files / 153 tests passed、frontend lint 与 Next build passed；主线合并后同一组定向回归仍为 70 passed。
- 继续拆分 Bilibili 收藏夹 API 边界：新增 `app/services/bilibili_favorites.py` 承接收藏夹列表、单页内容、全量分页、批量移动和失效内容清理；`app/services/bilibili.py` 保留原 `get_user_favorites`、`get_favorite_content`、`get_all_favorite_videos`、`move_favorite_resources`、`clean_favorite_resources` 兼容方法并委派到 helper，行数由约 493 行降至约 452 行。
- TDD 红灯已确认：`python -m pytest tests/test_bilibili_favorites.py -q` 在实现前因 `app.services.bilibili_favorites` 不存在失败；`python -m pytest tests/service_boundaries/test_auth_database_ingestion.py -q -k bilibili_service_delegates_favorite_helpers_to_service` 在实现前因 helper 文件不存在失败。
- 验证通过：定向回归 `python -m pytest tests/test_bilibili_service.py tests/test_bilibili_helpers.py tests/test_bilibili_favorites.py tests/test_content_fetcher.py tests/test_folder_ingestion.py tests/test_favorites_router.py tests/test_source_binding_favorites.py tests/service_boundaries/test_auth_database_ingestion.py -q` 为 66 passed；提交前完整验证 `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify-before-commit.ps1 -Format` 为后端 521 passed、前端 35 files / 153 tests passed、frontend lint 与 Next build passed。
- 继续拆分数据库启动与 SQLite 历史兼容迁移边界：新增 `app/services/sqlite_legacy_schema.py` 承接 legacy columns、SQLite identifier quoting、旧 `video_cache` 唯一 bvid 重建、scoped cache 补齐和索引创建；`app/database.py` 只保留 engine/session/init_db 与旧 `_ensure_sqlite_legacy_columns`、`_sqlite_*` alias 兼容入口，行数由约 436 行降至约 74 行。
- TDD 红灯已确认：`python -m pytest tests/service_boundaries/test_auth_database_ingestion.py -q -k database_delegates_sqlite_legacy_schema_to_service` 在实现前因 `app/services/sqlite_legacy_schema.py` 不存在失败；`python -m pytest tests/test_database_migration.py -q -k adds_knowledge_scope_columns` 在实现前因无法导入 `app.services.sqlite_legacy_schema` 失败。
- 验证通过：定向回归 `python -m pytest tests/test_database_migration.py tests/test_security_isolation.py tests/test_ingestion_tasks.py tests/test_imports.py tests/test_legacy_bilibili_sessions.py tests/service_boundaries/test_auth_database_ingestion.py -q` 为 46 passed；提交前完整验证 `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify-before-commit.ps1 -Format` 为后端 522 passed、前端 35 files / 153 tests passed、frontend lint 与 Next build passed。
- 继续拆分 RAG 问答编排边界：新增 `app/services/rag_qa.py` 承接空库/无命中 fallback、检索异常处理、上下文与来源构建、QA LLM 调用和 LLM 异常响应；`app/services/rag.py` 保留 `_fallback_answer`、`answer_question` 和 `_complete_answer` 兼容委派，继续专注向量写入、检索、摘要与 collection 对外方法，行数由约 451 行降至约 405 行。
- TDD 红灯已确认：`python -m pytest tests/test_rag_qa.py -q` 在实现前因 `app.services.rag_qa` 不存在失败；`python -m pytest tests/service_boundaries/test_knowledge_base_router.py -q -k rag_service_delegates_document_and_filter_helpers_to_services` 在实现前因 QA helper 文件不存在失败。
- 验证通过：定向回归 `python -m pytest tests/test_rag_qa.py tests/test_rag_service.py tests/test_rag_collection_ops.py tests/test_knowledge_scope.py tests/test_security_isolation.py tests/test_knowledge_base_scoping.py tests/test_knowledge_base_scope_build.py tests/test_folder_ingestion.py tests/test_imports.py tests/service_boundaries/test_knowledge_base_router.py -q` 为 81 passed；提交前完整验证 `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify-before-commit.ps1 -Format` 为后端 527 passed、前端 35 files / 153 tests passed、frontend lint 与 Next build passed。
- 继续拆分 ASR 本地音频准备边界：新增 `app/services/asr_audio.py` 承接 ffmpeg 探测、PCM/WAV 转码命令构造和 Recognition 输入格式选择；`app/services/asr.py` 保留 `_transcode_audio_to_pcm`、`_transcode_audio_to_wav`、`_prepare_recognition_input` 兼容委派，避免 ASRService 继续直接持有 `shutil.which` / `subprocess.run` 转码细节，行数由约 395 行降至约 333 行。
- TDD 红灯已确认：`python -m pytest tests/test_asr_audio.py -q` 在实现前因 `app.services.asr_audio` 不存在失败；`python -m pytest tests/service_boundaries/test_auth_database_ingestion.py -q -k asr_service_delegates_audio_preparation_to_service` 在实现前因音频 helper 文件不存在失败。
- 验证通过：定向回归 `python -m pytest tests/test_asr_audio.py tests/test_asr_config.py tests/test_content_fetcher.py tests/test_imports.py tests/service_boundaries/test_auth_database_ingestion.py -q` 为 52 passed；提交前完整验证 `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify-before-commit.ps1 -Format` 为后端 532 passed、前端 35 files / 153 tests passed、frontend lint 与 Next build passed。
- 继续拆分 ASR 远程转写运行时边界：新增 `app/services/asr_transcription.py` 承接 DashScope 结果 JSON 下载解析、API URL 构造、RESTful task 提交/查询、RESTful 同步轮询和 SDK 同步轮询；`app/services/asr.py` 保留 `_download_transcription`、`_build_api_url`、`_submit_transcription_task_restful`、`_fetch_transcription_task_restful`、`_transcribe_sync_restful`、`_transcribe_sync` 兼容委派，并保留 `urlrequest` alias 支持旧测试 monkeypatch，行数由约 333 行降至约 192 行。
- TDD 红灯已确认：`python -m pytest tests/test_asr_transcription.py -q` 在实现前因 `app.services.asr_transcription` 不存在失败；`python -m pytest tests/service_boundaries/test_auth_database_ingestion.py -q -k asr_service_delegates_transcription_runtime_to_service` 在实现前因远程转写 helper 文件不存在失败。
- 验证通过：定向回归 `python -m pytest tests/test_asr_transcription.py tests/test_asr_audio.py tests/test_asr_config.py tests/test_content_fetcher.py tests/test_imports.py tests/service_boundaries/test_auth_database_ingestion.py -q` 为 58 passed；提交前完整验证 `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify-before-commit.ps1 -Format` 为后端 538 passed、前端 35 files / 153 tests passed、frontend lint 与 Next build passed。

- 继续拆分 service boundary 测试维护边界：`tests/service_boundaries/test_auth_database_ingestion.py` 从 705 行混合边界文件收束为 17 行拆分哨兵，system auth、database、legacy B 站 session、ingestion、content fetcher、ASR 与 Bilibili service 边界守卫分别迁入 `test_system_auth_router.py`、`test_database_boundaries.py`、`test_legacy_bilibili_session_boundaries.py`、`test_ingestion_boundaries.py`、`test_content_fetcher_boundaries.py`、`test_asr_boundaries.py` 与 `test_bilibili_service_boundaries.py`。
- TDD 红灯已确认：`python -m pytest tests\service_boundaries\test_split_test_files.py -q -k auth_database_ingestion_boundary_tests_are_split_by_domain` 在实现前因缺少 `test_system_auth_router.py` 等聚焦文件失败；实现后该结构守卫转绿。
- 验证通过：定向回归 `python -m pytest tests\service_boundaries\test_split_test_files.py tests\service_boundaries\test_auth_database_ingestion.py tests\service_boundaries\test_system_auth_router.py tests\service_boundaries\test_database_boundaries.py tests\service_boundaries\test_legacy_bilibili_session_boundaries.py tests\service_boundaries\test_ingestion_boundaries.py tests\service_boundaries\test_content_fetcher_boundaries.py tests\service_boundaries\test_asr_boundaries.py tests\service_boundaries\test_bilibili_service_boundaries.py -q` 为 34 passed；`python -m pytest tests\service_boundaries -q` 为 81 passed；提交前完整验证 `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-before-commit.ps1 -Format` 为后端 540 passed、前端 35 files / 153 tests passed、frontend lint 与 Next build passed。

- 继续拆分 Bilibili 视频 API 边界：新增 `app/services/bilibili_video.py` 承接视频详情、AI 摘要、播放器 WBI/普通接口回退与音频 playurl WBI/普通接口回退；`app/services/bilibili.py` 保留 `get_video_info`、`get_video_summary`、`get_player_info` 与 `get_audio_url` 兼容方法并委派到 helper，行数由约 544 行降至约 467 行。
- TDD 红灯已确认：`python -m pytest tests\test_bilibili_video.py tests\service_boundaries\test_bilibili_service_boundaries.py -q` 在实现前因 `app.services.bilibili_video` 不存在失败；实现后新增行为测试和结构边界测试转绿。
- 验证通过：定向回归 `python -m pytest tests\test_bilibili_service.py tests\test_bilibili_helpers.py tests\test_bilibili_favorites.py tests\test_bilibili_video.py tests\test_content_fetcher.py tests\test_folder_ingestion.py tests\test_imports.py tests\test_asr_config.py tests\service_boundaries\test_bilibili_service_boundaries.py tests\service_boundaries\test_content_fetcher_boundaries.py -q` 为 55 passed；提交前完整验证 `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-before-commit.ps1 -Format` 为后端 545 passed、前端 35 files / 153 tests passed、frontend lint 与 Next build passed。

- 继续拆分 Bilibili 登录与身份 API 边界：新增 `app/services/bilibili_auth.py` 承接二维码生成重试、二维码图片 base64、二维码状态轮询、confirmed cookie 提取与当前登录用户 nav 查询；`app/services/bilibili.py` 保留 `generate_qrcode`、`poll_qrcode_status` 与 `get_user_info` 兼容方法并委派到 helper，行数由约 466 行降至约 385 行。
- TDD 红灯已确认：`python -m pytest tests\test_bilibili_auth.py tests\service_boundaries\test_bilibili_service_boundaries.py -q` 在实现前因 `app.services.bilibili_auth` 不存在失败；实现后 auth helper 行为测试和结构边界测试转绿。
- 验证通过：定向回归 `python -m pytest tests\test_bilibili_service.py tests\test_bilibili_helpers.py tests\test_bilibili_auth.py tests\test_bilibili_favorites.py tests\test_bilibili_video.py tests\test_auth_legacy.py tests\test_source_bindings.py tests\test_source_binding_services.py tests\test_source_binding_favorites.py tests\service_boundaries\test_bilibili_service_boundaries.py tests\service_boundaries\test_source_binding_router.py -q` 为 69 passed；提交前完整验证 `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-before-commit.ps1 -Format` 为后端 550 passed、前端 35 files / 153 tests passed、frontend lint 与 Next build passed。

- 继续拆分 ChatPanel 知识库上下文边界：新增 `frontend/components/chat/useChatKnowledgeContext.ts` 承接知识库 stats 加载、scope options 加载、知识库切换时聊天上下文重置、scope notice 计时与范围切换处理；`frontend/components/ChatPanel.tsx` 保留渲染、发送和组合职责，行数由约 508 行降至约 432 行。
- TDD 红灯已确认：`python -m pytest tests\frontend_structure\test_component_boundaries.py -q -k knowledge_context_hook` 在实现前因缺少 `useChatKnowledgeContext.ts` 失败；实现后结构守卫转绿，并约束 `ChatPanel.tsx` 不再直接调用 `knowledgeBaseApi.stats` / `knowledgeBaseApi.getScopeOptions` 或持有底层 scope/stats state。
- 验证通过：定向回归 `cd frontend; npm test -- ChatPanel.test.tsx ChatPanel.history.test.tsx ChatPanel.web-search-results.test.tsx ChatPanel.streaming.test.tsx ChatPanel.web-search.test.tsx ChatPanel.config.test.tsx` 为 6 files / 28 tests passed；`python -m pytest tests\frontend_structure -q` 为 45 passed；提交前完整验证 `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-before-commit.ps1 -Format` 为后端 551 passed、前端 35 files / 153 tests passed、frontend lint 与 Next build passed。

- 继续拆分 ChatPanel 视口与输入框 DOM 行为边界：新增 `frontend/components/chat/useChatPanelViewport.ts` 承接聊天滚动跟随、滚动帧取消、末尾锚点滚动和 composer textarea 高度自适应；`frontend/components/ChatPanel.tsx` 不再直接持有 `requestAnimationFrame`、`scrollIntoView`、`scrollHeight` 或 textarea style mutation，行数降至约 375 行。
- TDD 红灯已确认：`python -m pytest tests\frontend_structure\test_component_boundaries.py -q -k viewport_hook` 在实现前因缺少 `useChatPanelViewport.ts` 失败；实现后结构守卫转绿，并约束主组件不再回收这些 DOM 行为细节。
- 验证通过：定向回归 `cd frontend; npm test -- ChatPanel.test.tsx ChatPanel.history.test.tsx ChatPanel.web-search-results.test.tsx ChatPanel.streaming.test.tsx ChatPanel.web-search.test.tsx ChatPanel.config.test.tsx` 为 6 files / 28 tests passed；`python -m pytest tests\frontend_structure -q` 为 46 passed；`cd frontend; npm run lint` passed。

- 继续拆分知识库非流式回答适配边界：新增 `app/services/knowledge_base_answer_adapter.py` 承接 `_complete_knowledge_base_answer` 的 LLM completion、联网搜索工具链结果接入和 web search status 组装；`app/routers/knowledge_bases.py` 只保留兼容 alias，并通过运行时 resolver 保持旧 `_complete_llm_answer` monkeypatch 注入点可用。
- TDD 红灯已确认：`python -m pytest tests\service_boundaries\test_knowledge_base_router.py -q -k answer_completion_adapter` 在实现前因缺少 `knowledge_base_answer_adapter.py` 失败；实现后结构守卫转绿，并约束 router 不再定义完整 async adapter 或静态捕获 LLM completion。
- 验证通过：定向回归 `python -m pytest tests\service_boundaries\test_knowledge_base_router.py tests\test_chat_user_credentials.py tests\knowledge_base_web_search_api tests\knowledge_base_web_search_streaming tests\test_knowledge_base_web_search_tool_run.py tests\test_knowledge_base_web_search_fallback.py tests\test_knowledge_base_web_search.py tests\test_knowledge_base_streaming.py tests\test_knowledge_base_scoping.py -q` 为 70 passed。

- 继续拆分用户 AI 服务密钥面板边界：新增 `frontend/components/api-accounts/useApiAccountsPanel.ts` 承接密钥列表加载、表单初始值、服务商切换、创建/更新、设默认、验证与删除确认；`frontend/components/ApiAccountsPanel.tsx` 保留弹窗布局和字段渲染，避免安全配置 UI 继续同时持有 API CRUD 细节。
- TDD 红灯已确认：`python -m pytest tests\frontend_structure\test_component_boundaries.py -q -k api_accounts_panel_uses_state_hook` 在实现前因缺少 hook 失败；实现后结构守卫转绿，并约束面板不再直接调用 `apiAccountApi.*`。
- 验证通过：定向回归 `cd frontend; npm test -- ApiAccountsPanel.test.tsx ChatPanel.config.test.tsx` 为 2 files / 10 tests passed；`python -m pytest tests\frontend_structure -q` 为 47 passed；`python -m pytest tests\test_user_api_accounts.py tests\test_chat_user_credentials.py tests\test_security_isolation.py -q` 为 19 passed；`cd frontend; npm run lint` passed。

- 继续拆分导入弹窗边界：新增 `frontend/components/import-modal/useImportModal.ts` 承接导入方式加载、B 站二维码获取/轮询/超时、视频 URL 导入、本地视频上传和弹窗关闭状态复位；`frontend/components/ImportModal.tsx` 保留步骤布局与字段渲染，行数降至约 259 行，为后续学习复盘、导入源扩展降低耦合。
- TDD 红灯已确认：`python -m pytest tests\frontend_structure\test_component_boundaries.py -q -k import_modal_uses_state_hook` 在实现前因缺少 hook 失败；实现后结构守卫转绿，并约束弹窗不再直接调用 `importApi.*` / `sourceBindingApi.*` 或持有轮询计时器细节。
- 验证通过：定向回归 `cd frontend; npm test -- ImportModal.test.tsx api.test.ts` 为 2 files / 10 tests passed；`python -m pytest tests\frontend_structure -q` 为 48 passed；`python -m pytest tests\test_imports.py tests\test_ingestion_tasks.py tests\service_boundaries\test_ingestion_boundaries.py -q` 为 15 passed；`cd frontend; npm run lint` passed。

- 继续拆分前端组件边界结构测试：`tests/frontend_structure/test_component_boundaries.py` 从约 475 行收束为轻量哨兵，ChatPanel、账号/导入弹窗、Auth/Sources/Workspace 结构守卫分别迁入 `test_chat_component_boundaries.py`、`test_account_import_component_boundaries.py` 与 `test_auth_sources_workspace_component_boundaries.py`；新增 `test_split_component_boundary_files.py` 防止测试继续回流到单一大文件。
- TDD 红灯已确认：`python -m pytest tests\frontend_structure\test_split_component_boundary_files.py -q` 在实现前因目标拆分文件不存在失败；实现后该拆分守卫转绿。
- 验证通过：定向回归 `python -m pytest tests\frontend_structure -q` 为 50 passed。

- 继续拆分前端样式边界结构测试：`tests/frontend_structure/test_style_boundaries.py` 从约 516 行收束为轻量哨兵，全局样式入口、workspace/account/source/knowledge 样式聚合、chat/import 样式聚合分别迁入 `test_global_style_boundaries.py`、`test_workspace_account_source_style_boundaries.py` 与 `test_chat_import_style_boundaries.py`；新增 `test_split_style_boundary_files.py` 防止样式守卫继续回流。
- TDD 红灯已确认：`python -m pytest tests\frontend_structure\test_split_style_boundary_files.py -q` 在实现前因目标拆分文件不存在失败；实现后该拆分守卫转绿。
- 验证通过：定向回归 `python -m pytest tests\frontend_structure -q` 为 52 passed。

- 继续拆分知识库路由 service boundary 测试维护边界：`tests/service_boundaries/test_knowledge_base_router.py` 从 545 行混合边界文件收束为 15 行拆分哨兵；知识库 web search/LLM runtime、catalog/build、chat/search、delete、RAG/ingestion/favorite 默认目录边界守卫分别迁入 `test_knowledge_base_router_web_search_boundaries.py`、`test_knowledge_base_router_catalog_build_boundaries.py`、`test_knowledge_base_router_chat_search_boundaries.py`、`test_knowledge_base_delete_boundaries.py` 与 `test_rag_ingestion_boundaries.py`，并将通用 `function_source()` 上移到 `tests/service_boundaries/helpers.py`。
- TDD 红灯已确认：`python -m pytest tests\service_boundaries\test_split_test_files.py::test_knowledge_base_router_boundary_tests_are_split_by_domain -q` 在实现前因目标拆分文件不存在失败；实现后该拆分守卫转绿。
- 验证通过：定向回归 `python -m pytest tests\service_boundaries\test_knowledge_base_router.py tests\service_boundaries\test_knowledge_base_router_web_search_boundaries.py tests\service_boundaries\test_knowledge_base_router_catalog_build_boundaries.py tests\service_boundaries\test_knowledge_base_router_chat_search_boundaries.py tests\service_boundaries\test_knowledge_base_delete_boundaries.py tests\service_boundaries\test_rag_ingestion_boundaries.py tests\service_boundaries\test_split_test_files.py -q` 为 28 passed；`python -m pytest tests\service_boundaries -q` 为 86 passed；提交前完整验证 `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-before-commit.ps1 -Format` 为后端 561 passed、前端 35 files / 153 tests passed、frontend lint 与 Next build passed。

- 继续拆分知识库 scope/build 集成测试维护边界：`tests/test_knowledge_base_scope_build.py` 从 531 行收束为轻量哨兵，范围解析、scope options、构建请求和构建状态轮询用例分别迁入 `test_knowledge_base_scope_resolution.py`、`test_knowledge_base_scope_options.py`、`test_knowledge_base_build_requests.py` 与 `test_knowledge_base_build_status.py`；`test_split_test_files.py` 的旧“从 scoping 文件拆出”守卫也改为检查 focused 文件集合，避免二次拆分后误要求用例回流到中转文件。
- TDD 红灯已确认：`python -m pytest tests\service_boundaries\test_split_test_files.py::test_knowledge_base_scope_build_tests_are_split_by_domain -q` 在实现前因目标拆分文件不存在失败；实现后该结构守卫转绿。
- 验证通过：定向回归 `python -m pytest tests\test_knowledge_base_scope_build.py tests\test_knowledge_base_scope_resolution.py tests\test_knowledge_base_scope_options.py tests\test_knowledge_base_build_requests.py tests\test_knowledge_base_build_status.py tests\test_knowledge_base_scoping.py tests\test_knowledge_scope.py tests\test_knowledge_base_streaming.py tests\service_boundaries\test_split_test_files.py -q` 为 44 passed；`python -m pytest tests\service_boundaries -q` 为 87 passed；提交前完整验证 `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-before-commit.ps1 -Format` 为后端 563 passed、前端 35 files / 153 tests passed、frontend lint 与 Next build passed。

- 继续拆分 ChatPanel 联网搜索结果前端测试维护边界：`frontend/components/ChatPanel.web-search-results.test.tsx` 从 511 行收束为轻量哨兵，联网搜索来源展示、live progress 和状态诊断用例分别迁入 `frontend/components/chat/__tests__/ChatPanel.web-search-sources.test.tsx`、`ChatPanel.web-search-progress.test.tsx` 与 `ChatPanel.web-search-status.test.tsx`，共享基础 mock 返回值放入 `chatPanelWebSearchHelpers.ts`；旧结构守卫同步改为检查 focused 文件集合。
- TDD 红灯已确认：`python -m pytest tests\frontend_structure\test_split_chat_web_search_result_files.py -q` 在实现前因目标拆分文件不存在失败；实现后该结构守卫转绿。
- 验证通过：定向回归 `cd frontend; npm test -- ChatPanel.test.tsx ChatPanel.history.test.tsx ChatPanel.streaming.test.tsx ChatPanel.web-search.test.tsx ChatPanel.web-search-results.test.tsx ChatPanel.web-search-sources.test.tsx ChatPanel.web-search-progress.test.tsx ChatPanel.web-search-status.test.tsx ChatPanel.config.test.tsx` 为 9 files / 29 tests passed；`python -m pytest tests\frontend_structure -q` 为 53 passed；提交前完整验证 `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-before-commit.ps1 -Format` 为后端 564 passed、前端 38 files / 154 tests passed、frontend lint 与 Next build passed。

- 继续拆分 Tavily 联网搜索后端测试维护边界：`tests/test_web_search_tavily.py` 从 494 行收束为轻量哨兵，Tavily 成功路径、请求级 provider/API key override、fallback 与失败状态用例分别迁入 `tests/web_search_tavily/test_success_paths.py`、`test_overrides.py` 与 `test_fallbacks_and_failures.py`，共享 fake resolver 放入 `tests/web_search_tavily/helpers.py`。
- TDD 红灯已确认：`python -m pytest tests\service_boundaries\test_split_test_files.py::test_web_search_tavily_tests_are_split_by_domain -q` 在实现前因目标拆分文件不存在失败；实现后该结构守卫转绿。
- 验证通过：定向回归 `python -m pytest tests\test_web_search_tavily.py tests\web_search_tavily tests\web_search_service tests\test_knowledge_base_web_search.py tests\test_knowledge_base_web_search_tool_run.py tests\test_knowledge_base_web_search_fallback.py tests\service_boundaries\test_split_test_files.py -q` 为 56 passed；`python -m pytest tests\service_boundaries -q` 为 88 passed；提交前完整验证 `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-before-commit.ps1 -Format` 为后端 566 passed、前端 38 files / 154 tests passed、frontend lint 与 Next build passed。

- 继续拆分 source binding service 后端测试维护边界：`tests/test_source_binding_services.py` 从 483 行收束为轻量哨兵，绑定列表/撤销/active 校验、B 站凭据解析、二维码生成与轮询流程用例分别迁入 `tests/source_binding_services/test_binding_catalog.py`、`test_binding_credentials.py` 与 `test_bilibili_qrcode.py`，共享注册用户和绑定造数 helper 放入 `tests/source_binding_services/helpers.py`。
- TDD 红灯已确认：`python -m pytest tests\service_boundaries\test_split_test_files.py::test_source_binding_service_tests_are_split_by_domain -q` 在实现前因目标拆分文件不存在失败；实现后该结构守卫转绿。
- 验证通过：定向回归 `python -m pytest tests\test_source_binding_services.py tests\source_binding_services tests\test_source_bindings.py tests\test_source_binding_favorites.py tests\test_favorites_router.py tests\service_boundaries\test_source_binding_router.py tests\service_boundaries\test_split_test_files.py -q` 为 53 passed；`python -m pytest tests\service_boundaries -q` 为 89 passed；提交前完整验证 `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-before-commit.ps1 -Format` 为后端 568 passed、前端 38 files / 154 tests passed、frontend lint 与 Next build passed。

- 继续拆分 knowledge scope 后端集成测试维护边界：`tests/test_knowledge_scope.py` 从 409 行混合文件收束为轻量哨兵，知识库 catalog、RAG scoped filter、删除清理和向量失败后的数据库 fallback 用例分别迁入 `tests/knowledge_scope/test_catalog.py`、`test_rag_filters.py`、`test_delete_cleanup.py` 与 `test_db_fallback.py`，共享注册和知识库创建 helper 放入 `tests/knowledge_scope/helpers.py`。
- TDD 红灯已确认：`python -m pytest tests\service_boundaries\test_split_test_files.py::test_knowledge_scope_tests_are_split_by_domain -q` 在实现前因目标拆分文件不存在失败；实现后该结构守卫转绿。
- 验证通过：定向回归 `python -m pytest tests\test_knowledge_scope.py tests\knowledge_scope tests\service_boundaries\test_split_test_files.py::test_knowledge_scope_tests_are_split_by_domain -q` 为 12 passed；相关知识库 scope/build 回归 `python -m pytest tests\test_knowledge_scope.py tests\knowledge_scope tests\test_knowledge_base_scoping.py tests\test_knowledge_base_scope_build.py tests\test_knowledge_base_scope_resolution.py tests\test_knowledge_base_scope_options.py tests\test_knowledge_base_build_requests.py tests\test_knowledge_base_build_status.py -q` 为 30 passed；`python -m pytest tests\service_boundaries -q` 为 90 passed；提交前完整验证 `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\verify-before-commit.ps1 -Format` 为后端 570 passed、前端 38 files / 154 tests passed、frontend lint 与 Next build passed。

- 继续拆分知识库联网搜索 fallback 集成测试维护边界：`tests/test_knowledge_base_web_search_fallback.py` 从 405 行混合文件收束为轻量哨兵，数据库 fallback 隔离、initial web context 和知识库无命中时的 web-only 回答用例分别迁入 `tests/knowledge_base_web_search_fallback/test_db_fallback_isolation.py`、`test_initial_web_context.py` 与 `test_web_only_answer.py`，共享 fake document、fake completion response 和 web search tool call 构造放入 `tests/knowledge_base_web_search_fallback/helpers.py`。
- TDD 红灯已确认：`python -m pytest tests\service_boundaries\test_split_test_files.py::test_knowledge_base_web_search_fallback_tests_are_split_by_domain -q` 在实现前因目标拆分文件不存在失败；实现后该结构守卫转绿，同时旧“从 scoping 文件拆出”守卫改为检查 focused 文件集合，避免要求用例回流到中转文件。
- 验证通过：定向回归 `python -m pytest tests\test_knowledge_base_web_search_fallback.py tests\knowledge_base_web_search_fallback tests\service_boundaries\test_split_test_files.py::test_knowledge_base_web_search_fallback_tests_are_split_from_scoping_file tests\service_boundaries\test_split_test_files.py::test_knowledge_base_web_search_fallback_tests_are_split_by_domain -q` 为 7 passed；联网搜索相关回归 `python -m pytest tests\test_knowledge_base_web_search_fallback.py tests\knowledge_base_web_search_fallback tests\knowledge_base_web_search_api tests\knowledge_base_web_search_streaming tests\test_knowledge_base_web_search.py tests\test_knowledge_base_web_search_tool_run.py tests\test_knowledge_base_web_search_compat.py -q` 为 37 passed；`python -m pytest tests\service_boundaries -q` 为 91 passed。
